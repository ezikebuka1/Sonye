# Active Context

Snapshot of *now* — kept lean. History lives in git; load-bearing rules live in `m3-closeout.md` and `systemPatterns.md`.

## Current focus

**v1 is BUILD-COMPLETE and pushed to origin/main. Auth is LIVE on Twilio Verify — cutover done + verified 2026-06-30.** No remaining feature work. **Remaining before public launch: only the recommended Texas attorney review of Terms §7 release + §12 arbitration (not strictly blocking).** Going live is now a business call, not a technical one. Attendance SMS + its cron stay DORMANT (v1.1, its own A2P campaign). Trust progress.md + this file.

## Recently shipped (on origin/main)

**Brand assets — Sonye "s." icon set + OG share image; scaffold sweep. SHIPPED ✅.** Replaced the Next scaffold favicon with the approved option-L "s." mark and gave the site a real share image. **Generator** `scripts/generate-brand-assets.mjs` (+ vendored `scripts/brand-paths.mjs`): fetches Baloo 2 ExtraBold(800) + Nunito Sans(400) as STATIC TTFs from Google Fonts at gen time (legacy-UA css2 trick pins weight 800 as a parseable .ttf — not a variable font / woff2 / EOT), path-converts ALL text at gen time (the "s" glyph + "sonye" wordmark in Baloo 2; the OG subline in Nunito Sans) and vendors the extracted path DATA into brand-paths.mjs so regeneration is fully offline; TTFs never committed. Deterministic + idempotent (offline rerun → byte-identical assets, md5-confirmed). **Emits into src/app/ (Next auto-serves all four — no manual `<link>`s):** icon.svg (rounded rx=22 wash tile, path-only mark, `<text` count 0), favicon.ico (16+32+48 — `file` shows 3 icons), apple-icon.png (180×180 square), opengraph-image.png (1200×630: wash bg, mark tile, "sonye." wordmark w/ coral period, steel-aa `#4A6B8C` subline "Curated pickleball games in Dallas."). **Two opentype.js v2 bugs routed around (documented in-script):** translated `glyph.getPath` corrupts composite glyphs, and `Path.toPathData`'s optimizer emits NaN/undefined — so glyphs are extracted at the ORIGIN, translated + serialized by our own code, with a NaN/undefined guard that fails the build on regression. **layout.tsx:** added `metadataBase: new URL("https://sonye.app")` (og:image now unfurls absolute — confirmed in the injected tag); **description rewritten per RIDER R3** → "Curated pickleball games with reliable groups, at Dallas public courts." (retires the v1-overpromising "casual sports" plural that would render next to the pickleball OG image); title "Sonye" unchanged. **devDeps join the commit per R2:** opentype.js + png-to-ico — png-to-ico swapped in for to-ico PRE-COMMIT (AMENDMENT [ICO-1]) so the tree ships audit-clean: to-ico dragged 91 transitive packages incl. the deprecated `request` stack (`request` 2.65 / `jimp` 0.2.21 / `tough-cookie` 2.5.0) → 17 npm-audit vulns (5 critical) in a devDep that renders one .ico; png-to-ico rasterizes the same 16/32/48 buffers through a tiny `pngjs`/`minimist` tree that adds ZERO vulns. `npm audit` 17 → 5 — the 5 survivors are pre-existing FRAMEWORK advisories (next / postcss / @babel/core / js-yaml / brace-expansion), untouched by and unrelated to this swap; `node_modules/request`/`jimp`/`tough-cookie` lockfile greps all 0. Both build-time only, nothing in the shipped bundle. **Deleted 5 unused scaffold svgs** from public/ (next/vercel/globe/file/window — recon confirmed 0 refs in src/; optin.png + fonts/ untouched). **VERIFIED (raw output):** `npm run build` EXIT 0 + `tsc --noEmit` EXIT 0; prod `next start` GET of the four routes → all 200 (image/x-icon, image/svg+xml, image/png×2; 14510 / 1113 / 3594 / 36125 B); anon `/` head carries Next-injected `<link rel=icon>` (favicon 48x48 + icon.svg any) + `apple-touch-icon` 180x180 + absolute og:image/twitter:image 1200×630; grep -c `<text` icon.svg = 0; scaffold-name grep in src/ = 0; git status shows the 5 deletions. Parked at HARD STOP — Ebuka pushes.

---
**Pre-launch UX polish — Squad stub removed, create-slot exits, host-neutral wall. SHIPPED ✅.** BottomTabBar: Squad tab removed for launch (TabKey narrowed to home|profile; unused `Users` import dropped; zero usages confirmed; two stale "Squad" comments reworded generically so the tab-scoped grep is 0 — the div-fallback scaffold stays for when the stub returns, v1.1 list stands). Create-slot success: raw-JSON confirmation → human summary (day · time · venue via format-central `formatCentral`, no new query — reuses the already-fetched row) + 44px neutral outlined "Done" → /dashboard + "Create another" retained + form-page "← Dashboard" back link (the form previously dead-ended). Lobby wall host-neutral: host pill (`HostTag`) removed from LobbyWall AND `isHost` stripped from the group-lobby/page.tsx server mapping — the flag never reaches the client payload; the server-only `WallRow` type keeps its `is_host` field (accurate RPC typing, TS-erased); `slot_wall` RPC untouched; "you" pill + owner Remove unchanged (Remove keys off the VIEWER being owner via `is_owner()`, fully independent of the author flag — e2e HOST REMOVE stays green). VERIFIED: build EXIT 0 + tsc clean; tab-scoped squad greps 0 (`key: "squad"` 0, BottomTabBar 0 — brand tagline in HeroText + layout metadata untouched by design); `isHost` 0 in src/, `is_host` confined to the group-lobby WallRow RPC typing (server-only); payload grep clean on an owner-posted wall viewed by a joined non-owner — `is_host`/`isHost`/`>host<` all 0 in the hydrated DOM AND the raw server document, owner message still rendered (proving the `is_host=true` row flowed through), only `#DCEBFF`/`#15457B` hits are the legit Beginner skill badge (authed fetch ran on the dev server — `/dev-login` is prod-`notFound()`; RSC field-set identical, prod build separately verified EXIT 0 + tsc clean); device @390 all three fixes (tabs = Home+Profile; create-slot → "Wed, Jul 7 · 7:10 PM at Churchill Park — it's live on the feed" → Done lands on /dashboard; wall owner message plain name+avatar, no pill); lobby-wall 7/7 + player-leave 11/11 green (prior red was dirty-DB pollution from an aborted run, cleared; no test edits this dispatch).

---
**Profile tab activated — `/profile` account screen + sign-out. SHIPPED ✅.** Server-rendered `/profile` (D13 all-server, `force-dynamic`): gate → `redirect('/auth')` (onboarding pattern); row-less authed viewer → `/onboarding`; **self-pinned** `.eq("id", uid)` select of `first_name, phone, role, created_at` (owner passes `is_owner()` — an unpinned select returns every row); server-side phone mask, last-4 only (full number verified ABSENT from the signed-in payload); `getAvatar(null, users.id)` blue family (D7.3 — gender color is lobby-only); "Member since" via America/Chicago Intl formatter (format-central conventions); owner-only "Owner dashboard" row when `role='owner'`; neutral outlined Sign out ≥44px (no coral, no red); Privacy · Terms footer. `signOutAction` colocated (`profile/actions.ts`): server client → `signOut()` → `redirect('/')` (lands on the D20 public feed). BottomTabBar: Home (`/`) + Profile (`/profile`) wired as real `next/link` Links; Squad stays an inert stub; active state prop-driven. **D16 nav rider REMOVED — as built it lived in BottomTabBar.tsx, NOT the D16 doc's "global-header" (doc annotated 2026-07-02 — see D16 as-built note)** — full `isOwner` thread cleanup (BottomTabBar prop + `LayoutDashboard` import, HomeClient forward, page.tsx dead `is_owner()` fetch); post-condition: the Profile row is the ONLY dashboard entry point outside the route itself. Rider 1E: logged-out redirects on `/dashboard` + `/create-slot` switched `/dev-login` → `/auth` (`/dev-login` is `notFound()` in prod; sign-out made the logged-out-owner path live). `data-testid`: `nav-dashboard` → `profile-owner-dashboard`. **VERIFIED (prod build, device @390):** build exit 0, tsc clean, `/profile` in route table; greps — zero client-side supabase in `profile/`, zero `isOwner` remnants; owner sees row, Sign out lands logged-out on `/`, `/profile` re-gates; non-owner sees no row; signed-out `/dashboard` + `/create-slot` → 307 `/auth`. e2e: `player-leave.spec.ts` NAV test rewritten in place to the consolidated placement (owner row on `/profile` with `href=/dashboard`, `nav-dashboard` count 0 on `/`; player sees no row). Battery: player-leave 11/11 (incl. NAV), lobby-wall 7/7, twilio-consent 6/6 — green; home-feed + dashboard-cancel each carry one red VERIFIED PRE-EXISTING by stash re-run on clean main (stale pre-D20 anon-`/`-redirect assertion; fixture joins 14 tester phones never present in seed.sql). Repair deferred to its own dispatch.

---
**D19 — join_slot past-game guard + "started" terminal state. SHIPPED ✅.** Closes the integrity hole where a started/ended (non-cancelled) game stayed joinable via a direct URL / stale feed (burned the D9 daily cap, polluted rosters, seated a dead game). **3 commits on `main`:** (1) decision doc `D19`; (2) migration `20260624120000_join_slot_past_game_guard.sql` — surgical `CREATE OR REPLACE join_slot` adding ONE guard `IF v_starts_at <= now() THEN RAISE 'join_slot: slot % has already started' USING ERRCODE='object_not_in_prerequisite_state'`, placed AFTER the cancelled check + BEFORE the D9 civil-date derivation; reads the already-locked `v_starts_at` (no 2nd SELECT, no `::date`/`AT TIME ZONE`), fully orthogonal to D9; body byte-identical elsewhere; D17 grants re-asserted. (3) frontend — substring `'already started'` wired through ALL THREE join callers (`joinSlotAction`→`{error:'started'}`; `join/page.tsx` + `onboarding/actions.ts`→`redirect('/?toast=started')` — the onboarding leg specifically prevents the false-success `welcomed` toast) + `HomeClient` (`startedIds` Set mirroring `cancelledIds`; TOAST_MAP `started:"Game already started"`, D5 no period) + `SlotCard` new `started` branch (non-interactive `<div>`, `bg-inset` + lucide `Clock` + "Already started", full contrast, `cursor-not-allowed select-none`; precedence cancelled→started→membership→pending→full→open). **A11y fix bundled:** removed `opacity-80` from the existing cancelled `<div>` so its status text holds full contrast — both terminal footers now opacity-1 and visually distinct (started=`bg-inset`+clock "past"; cancelled=`#EEF2F8` "erased"). **VERIFIED:** SQLSTATE of `object_not_in_prerequisite_state` is **55000** (dispatch said 22023 — that's `invalid_parameter_value`; corrected). phase3a green — GUARD-A (fires, `ERROR 55000 … has already started`), GUARD-B (future slot still `joined`), GUARD-C (D9 `23505` still fires on a FUTURE slot, NOT the started error → orthogonality). **Pre-existing P4/P5 verify identically; their stale `2026-06-13` D9/tz seed slots were refreshed to `2027-06-13`** — they'd aged PAST `now()=2026-06-24` which the new guard rejects; same Dallas day, same tz-crossing. phase3b P6 race re-run green (guard no-op on future S_race: Grace `joined`, Henry blocked ~3.5s→`waitlisted`). `npm run build` exit 0, tsc clean, route table intact; live @390 render proof via temp `/dev-d19` harness (since DELETED) confirmed the started footer (clock, full contrast, non-interactive `<div>`) + the de-dimmed cancelled footer. **KNOWN PRE-EXISTING (NOT D19):** phase3a P1-B (anon `claim_lookups`) is red — D17 `grant_matrix_lockdown` moved `claim_lookups` to Bucket C (internal/definer-only) and revoked anon EXECUTE; no app caller relies on it; annotated in-place + tracked separately (re-green is a D17 follow-up).

---
**D11-B2 — Vercel Cron attendance dispatcher (the SENDER). SHIPPED ✅ (built; stays DORMANT until v1.1 — see Current focus).** Net-new app code on top of B1's DB layer (D18: `claim_attendance_dispatch` service_role-only RPC + `attendance_sms_sent_at` dedup, 2h–26h window). **Commit 1 (service client + transport seam):** `src/lib/supabase/service.ts` — `createServiceClient()` mirrors `anon.ts` (direct `@supabase/supabase-js` `createClient`, `persistSession:false`) but with `SUPABASE_SERVICE_ROLE_KEY` (server secret, never `NEXT_PUBLIC_*`, never logged) + a top-of-file `import 'server-only'` guard so any client-component import fails at build (the `server-only` npm pkg was absent → added as a dep). `src/lib/sms/transport.ts` — one `sendSms({to,body})` ENV-AWARE seam: dev (`NODE_ENV!=='production'`) `console.log`s a labeled block (to + body, body carries the /c/y + /c/n links) and does NOT call Twilio; prod POSTs the Twilio Messaging API via minimal `fetch` (Basic auth `accountSid:authToken`, `MessagingServiceSid`); non-2xx throws so the caller counts+skips. Also `import 'server-only'` (touches Twilio secrets in prod). **ISOLATION:** `grep -rn "supabase/service" src/` → the service client is imported ONLY by the cron route, nowhere else. **Commit 2 (route + cron schedule):** `src/app/api/cron/attendance/route.ts` (GET, `runtime='nodejs'`, `dynamic='force-dynamic'`) — (a) 401 unless `Authorization === Bearer ${CRON_SECRET}` (unset secret → 401, never unguarded; same bearer lets Ebuka force-run for the smoke test); (b) candidate scan via the isolated service client (`ends_at < now()-2h AND ends_at > now()-26h AND cancelled_at IS NULL` — coarse pre-filter, RPC re-checks under lock); (c) per slot `rpc('claim_attendance_dispatch',{p_slot_id})`, per returned row build `${base}/c/y/${token}` + `/c/n/${token}` from `NEXT_PUBLIC_BASE_URL` (fallback localhost:3000) and `sendSms` with the A2P copy (`Hi ${first_name}, did you make it to today's game? Yes: … No: … Reply STOP to opt out.`), one bad number caught+counted, never aborts the batch; (d) returns `{slotsProcessed, messagesSent, errors}` ONLY — never tokens/phones. `vercel.json` — hourly cron `0 * * * *` → `/api/cron/attendance` (matches ends_at+2h granularity, 26h recovery) **[SUPERSEDED 2026-06-23: this Vercel cron was REMOVED to unblock the Vercel Hobby deploy — `vercel.json` now `{}`; the route is instead triggered by a free GitHub Actions scheduled workflow at go-live, see Just shipped]**. **VERIFIED locally (`db reset` + D18 fixtures):** `npm run build` EXIT 0, tsc clean, `/api/cron/attendance` in route table; no-bearer + wrong-bearer → 401; valid bearer → 200 `{slotsProcessed:1, messagesSent:2, errors:0}` (S_PAST only; S_OLD/S_SOON/S_CXL excluded; waitlisted+left absent); dev transport logged 2 `[sms:dev]` blocks with the /c/y + /c/n links (no Twilio); second run → `messagesSent:0` (RPC dedup, still 2 blocks total). No secret (service-role key / Twilio token / CRON_SECRET) is logged or returned. **ENV FOR EBUKA (Vercel Production, part of cutover not local dev):** `SUPABASE_SERVICE_ROLE_KEY`, `TWILIO_ACCOUNT_SID`/`_AUTH_TOKEN`/`_MESSAGING_SERVICE_SID`, `CRON_SECRET`.

---
**D10 Amendment B — lobby wall UI (in-app coordination). SHIPPED ✅ (on origin/main).** The final wall piece on top of the 3d87fa6 schema (chat_messages joined-only RLS + `owner_delete_message` + anon write-revoke). **Pre-flight caught a broken spec premise:** the dispatch said "resolve author by matching message.user_id against slot_roster," but `slot_roster` returns `membership_id`, NOT `user_id`, and a joined non-owner is own-row-only on BOTH bridges (`session_memberships` SELECT = `user_id=current_user_id() OR is_owner`; `users` SELECT = `auth_user_id=auth.uid() OR is_owner`) — so a player could resolve only "You", every peer fell to "Player". **Architect chose Option 1 → new `slot_wall()` reader.** **Two-Commit:** (1) **schema [`9984d81`]** — `slot_wall(target_slot)` SECURITY DEFINER, `search_path=''`, audience gate `(is_joined_member OR is_owner)` in WHERE, REVOKE…PUBLIC / GRANT…authenticated; returns each message's author first_name+gender + `is_host` (author = slot `created_by`) + `is_self`; READ-only, no table/policy/data change; mirrors `slot_roster` exactly (incl. the harmless default `anon|EXECUTE` the internal gate neutralizes — same as slot_roster/owner_delete_message). (2) **UI** — see Just shipped. **TOKEN DECISION (pre-flight grep):** `--color-success #4BAE78` AND `--color-error #D64B4B` BOTH already exist (error is the established destructive red — LeaveSheet `bg-error`, LeaveGameControl `text-error`), so the "one sanctioned new token" clause did NOT fire — **`globals.css` untouched**; canned-chip checks use `text-success`, host Remove uses `text-error`, coral (`bg-coral`) is on Send ONLY. **Composer + canned taps gated to `viewer==='joined'`** (RLS only lets joined members insert; the host/owner-viewer is not a joined member → read + Remove only, NO composer — faithful to "RLS enforces joined+self at the DB"). **`ends_at + 2h` gate enforced in BOTH places:** client (`group-lobby/page.tsx` renders the closed card and fetches a COUNT only, never message content) AND server (`postLobbyMessageAction` re-fetches `ends_at`, rejects past+2h, inserts nothing — the necro guard). Host Remove → `removeLobbyMessageAction` → `owner_delete_message` → `router.refresh()`. **DEFERRED (out per dispatch, do NOT build): RIGHT NOW counts, stale-dimming, tap-plus-text, host pinning.**

---
**D10 Amendment A — peer phone redaction (owner-only). SHIPPED ✅ (on origin/main).** Closes the M5+D9 number-harvesting hole: the original D10 reveal let any joined player read every co-member's phone. Two commits. **Commit 1 (db):** migration `20260618054102_redact_peer_phone_owner_only.sql` — `CREATE OR REPLACE slot_roster` byte-identical to `20260613001515` except the phone `CASE`, which drops the `is_joined_member(target_slot) OR` disjunct, leaving `is_owner()` as the SOLE reveal predicate (return shape unchanged; grants re-asserted `REVOKE…PUBLIC` / `GRANT EXECUTE…authenticated`). Security boundary lives in the RPC — a non-owner caller never receives a peer phone, so the UI has nothing to leak. Proofs `phase3b_proofs_d10.sql`: **P9 INVERTED** (joined NON-owner racer now `joined_with_phone=0, joined_no_phone=6`), P10 unchanged (waitlisted `7/0/7`), P11 unchanged (owner `joined_with_phone=6`). Full battery green on a fresh reset; **TEETH** green — (a) joined non-owner JWT→`slot_roster`→all phones NULL, (b) same JWT→`SELECT phone FROM users WHERE id=<peer>`→**0 rows** (users RLS `(auth_user_id=auth.uid()) OR is_owner()` is own-row-only for a non-owner), (c) owner JWT→joined phones present. **Commit 2 (lobby):** `src/app/group-lobby/page.tsx` — `showSms` gate tightened to `Boolean(row.phone)` (was `!== null`; already conditional, so the redaction declaratively hides numbers from players and preserves them for the owner); `dirHelp` gated to `viewer==='owner'` (owner keeps "tap a number to text…"; players/waitlisted get neutral `"who's playing this game"` — no false number promise). No literal D10 disclosure string existed to remove; the realized copy was `dirHelp`. e2e `e2e/player-leave.spec.ts` (+2 cases, scoped to `a[aria-label^="Text "]` to dodge the support-link + RSC-double-emit confound): **D10A-PLAYER** (P2 joined on full → roster renders, peer-link count `=0`) + **D10A-OWNER** (owner on same slot → peer-link count `>0`). **DRIFT FLAG (for architect):** the D10-A *decision doc* was NOT actually committed before this dispatch — `decisions/D10-lobby-communication.md` and the index line below still record the ORIGINAL "joined members see each other's phones" decision. Land the D10-A amendment doc + correct the index line before/at merge.

---
**M5 (D16) — player-leave flow + owner dashboard nav rider. SHIPPED ✅ (on origin/main).** UI-ONLY dispatch (verified against the live `leave_slot`: the `found_other_game` code, the `leave_reason_note` column, and full/locked leaving all pre-exist — NO schema change; the banked `REVOKE UPDATE ON slots FROM anon` stays banked). `leaveSlotAction` (in `actions.ts`) mirrors `cancelSlotAction` exactly: server client → `rpc('leave_slot', {p_slot_id, p_leave_reason_code, p_leave_reason_note})` (RETURNS boolean → success = no error), RAISE-map on the actual texts (`not authenticated`→`/auth`, `no active membership`→`not_joined`, `not found`→`not_found`, `invalid or reserved leave reason`→`invalid_reason`, else `unknown`); the chip code + the optional note are stored **SEPARATELY** (NOT composed into one string like CancelSheet). `LeaveSheet` (net-new, modeled on CancelSheet's Toast-overlay mechanics — fixed scrim + slide-up, white note field) shows dynamic consequence copy on `waitlist_count` (>0 "goes to the next player on the waitlist" / =0 "opens up for someone else" + the steel "you'll be free to join another game today" — NO "can't be undone", NO reschedule), a REQUIRED single-select **radiogroup** (`role="radiogroup"`/`role="radio"`/`aria-checked`, arrow-key nav, **ZERO `aria-pressed`** — the deliberate departure from CancelSheet's toggles; sky-#8DBCF1 selection), red "Leave game" (disabled until a reason picked) + quiet steel "Keep my spot". `LeaveGameControl` (net-new lobby client island, mirrors DashboardClient's confirm wiring) owns the sheet + toast + `router.refresh()` (no optimistic mutation). **Entry point:** group lobby (`/group-lobby`), the viewer's OWN joined membership only, gated `viewer==='joined' && starts_at > now()` (strict — NO owner 2h grace; capacity is NOT a gate, per the D7 amendment). Lobby query now also reads the real trigger-managed `member_count`/`waitlist_count` from `slots` for the sheet props. **Rider:** owner-only `/dashboard` link appended to `BottomTabBar` (the app's `<nav>`), gated by `is_owner()` fetched in `page.tsx` → threaded `HomeClient`→`BottomTabBar`; hidden for non-owners. **DEFERRED:** waitlist-leave (same sheet, no promotion) + the Home-feed leave entry point. Decision doc **D16** (already committed to `main` as the docs half of Two-Commit; amends D7 line 28 "no leaving once locked"). Two-Commit: DOCS (D16, on main) / APP+tests+activeContext (this branch commit).

---
**Twilio Verify pivot — login OTP moved off the A2P campaign. SHIPPED ✅.** The A2P 10DLC campaign hit a STRUCTURAL 30923 (SMS-login-as-sole-opt-in can't qualify as voluntary opt-in). Login OTP pivoted to **Twilio Verify** (its own managed opt-out channel — no A2P campaign needed). **Recon-confirmed ZERO code change:** the cutover is a Supabase Cloud dashboard SID swap (Messaging SID → Verify Service SID) — GoTrue still mints the JWT, so the JWT `+`-normalization migration (`20260610120000`) + `signup_claim` survive untouched; login uses only `signInWithOtp`/`verifyOtp`; the ONLY Twilio call in `src/` is the DEFERRED attendance transport (`src/lib/sms/transport.ts`, dormant). Runbook in `cutover.md` §1. **Attendance SMS deferred to v1.1** (the only message type still needing its own A2P campaign).

---
**D20 — public feed front door (logged-out `/`). SHIPPED ✅.** `get_public_feed` RPC (anon-safe `SECURITY DEFINER`, inlined 50%-fill mask, no owner PII, `starts_at > now() AND cancelled_at IS NULL`, D17 Bucket-A grants) + `PublicFeed` server component + `page.tsx` routing split (session → `HomeClient`, else → `PublicFeed`) + `PublicSlotCard` (render-only; "Join this game" → `/auth?slotId=`; 50% rule). Demo seed: 5 future slots, 6 demo players (`+15555551001`–`1006`), trigger-computed `member_count` 1/4/6/3/2. **VERIFIED RLS REALITY (keep in mind):** anon has table SELECT but NO anon RLS policy on `slots` → a direct `from('slots')` returns **0 rows SILENTLY (not an error)** → the `get_public_feed` RPC is the ONLY anon data path.

---
**/auth enrichment — Flow-2 context banner + Flow-1 strip + trust lines. SHIPPED ✅.** Flow-2 "you're joining" context banner (full-FIRST three-state fill clause: full → waitlist wording with no count / ≥50% → "N spots left" / <50% masked → no clause; gender tag for non-open; a cancelled slot → falls back to Flow-1) + Flow-1 how-it-works strip + trust lines. **SMS consent disclosure BYTE-IDENTICAL / untouched** (`consent.ts`, the `data-testid="sms-consent"` block).

---
**Card visual cleanup — hero serif + tabular-nums + 44px CTAs + neighborhood. SHIPPED ✅.** Hero day/time → `font-serif` (Baloo 2) + `tabular-nums`; Join CTAs → `min-h-[44px]` WCAG (3 join CTAs + the in-flight pending state; the 4 D19 terminal/membership footers LEFT untouched); neighborhood shown on public cards (`string | null`, defensive render). Edited in BOTH `SlotCard` + `PublicSlotCard` (duplicated literals, **NO shared constant** — preserves the D20 isolation boundary). **D19 terminal states verified untouched.**

---
**D21 — safety-persistence gate verified SATISFIED ✅.** The legal-publish gate (lobby-wall message retention disclosed for safety): the privacy policy already accurately discloses lobby-message retention-for-safety — **no fix needed, gate cleared**. See `decisions/D21`.

---
**Prior — Phase 5 Dispatch 2 (2026-06-16) — owner dashboard + cancel sheet, BUILT & LIVE-VERIFIED locally (now on `main`).** Net-new `/dashboard` route (no owner dashboard existed; create-slot was the only owner page, URL-only). Server component gates (`getUser`→`/dev-login`, `is_owner`→access-denied) + fetches the owner's own not-cancelled slots with a **2h-padded** lower bound (`starts_at > now()-2h`, so a started-but-pre-attendance game stays cancellable until `starts_at+2h`; D14 cancel has no time guard) and selects **`waitlist_count`** (real trigger column the feed never read). `DashboardClient` renders cards (reused `skillBadge` ramp — now exported from SlotCard; net-new always-on fill dots, NOT the 50%-gated SocialProofBlock; waitlist demand nudge = neutral inset + coral "Open another slot" only when full+wl>0; cancel = quiet red text link). `CancelSheet` (net-new, modeled on Toast's overlay mechanics — fixed scrim + slide-up, white note field) shows **dynamic consequence copy** from member/waitlist counts (frees the daily cap / clears the waitlist; singular+plural; "This can't be undone."), a REQUIRED 2×2 reason picker (sky-#8DBCF1 selection, NOT beginner tint), composes one free-text `cancellation_reason` (`chip — note`). `cancelSlotAction` (in `actions.ts`) mirrors `joinSlotAction` exactly: server client → `rpc('cancel_slot', {p_slot_id, p_cancellation_reason})` (RETURNS boolean → success = no error), RAISE-map on the actual migration texts (`already cancelled`/`owner only`/`reason`/`not authenticated`). Post-cancel = `router.refresh()` (no optimistic row mutation; cancelled slot drops via the `cancelled_at IS NULL` filter). `formatCentral` extracted to `src/lib/format-central.ts` (shared by Home + dashboard). **DEFERRED (not built): attendance-review queue + owner_set_attendance.** Decision doc **D15**. **Two CLAUDE.md drifts logged in D15:** live coral = `#EE5E00`/`--color-cta` (not #D4724A); live fonts = Nunito+Baloo2 (not DM Sans/Instrument Serif). Two-Commit: DOCS (D15) / APP+tests+activeContext.

## Just shipped

- **D24 — tappable venue line → Google Maps (courts pin) + venue coordinates schema. BUILT & VERIFIED locally, COMMITTED at HARD STOP. NOT pushed — Ebuka deploys (`supabase db push` FIRST, then `git push`). Diff read + cleared by Architect.** Realizes the v2-signal "clickable venue → maps." **Two commits:** (1) **schema** — migration `20260715120000_d24_venue_coordinates.sql`: `venues.lat`/`lng` (`double precision`, nullable, no default) + three CHECKs (`venues_lat_range` -90..90, `venues_lng_range` -180..180, `venues_coords_paired` `(lat IS NULL)=(lng IS NULL)`); courts-level coords set for cole-park (32.812312, -96.793444), churchill-park (32.918421, -96.782705), lake-highlands-north (32.885735, -96.730095); `slot_share_preview` **DROP+CREATE** from the verbatim post-D12 body with `venue_lat, venue_lng` appended as the LAST two RETURNS-TABLE cols + `v.lat, v.lng` after `s.skill_level` (body otherwise byte-identical; DROP required because PG forbids CREATE OR REPLACE on a RETURNS-TABLE change — the m3_4 pattern, and the verbatim body itself opens with DROP); grants re-asserted (D17 Bucket A: `REVOKE…PUBLIC` / `GRANT…anon, authenticated`). Applied LOCALLY via docker exec — **NO db push**. (2) **code** — new `src/lib/maps-url.ts` `courtMapsUrl(lat,lng)` → `https://www.google.com/maps/search/?api=1&query=<lat>%2C<lng>`; `SlotPreview` gains `venue_lat`/`venue_lng: number | null` (TYPE ONLY — `fetchSlotPreview` whole-row cast + `SKILL_DISPLAY` OG-quarantine untouched); `/slot/[id]` venue line → full card-row `<a>` (leading MapPin 16 + venue text underlined in steel `decoration-[#4A6B8C]` + trailing ArrowUpRight 14, all `#4A6B8C`, ink `#14304D` venue text; `target=_blank rel=noopener noreferrer`, aria-label "Open {venue} in Google Maps (opens in a new tab)"), honesty subline UNCHANGED directly below; group-lobby venue `<h1>` **content** wrapped in the same `<a>` + one trailing ArrowUpRight ~19px `#4A6B8C` (the sole-h1 landmark + ink Baloo styling preserved, NO underline / NO MapPin); NULL coords → plain rendering on both surfaces. **Steel underline `decoration-[#4A6B8C]` KEPT per Architect ruling** (initially flagged as banked debt, then confirmed into this commit — the debt is closed, no follow-up needed). **VERIFIED (raw):** local migration apply clean (2×ALTER, 3×`UPDATE 1`, DROP/CREATE, REVOKE/GRANT); `SET ROLE anon` → `slot_share_preview(<Cole slot>)` returns `32.812312 / -96.793444`; `lat=999` → `venues_lat_range` reject, `lat=NULL` (lng set) → `venues_coords_paired` reject (no mutation either); `\df+` ACL = anon + authenticated EXECUTE, no PUBLIC (`service_role=X` is the ambient Supabase default, matches peer Bucket-A funcs get_public_feed/slot_roster/slot_wall); `tsc --noEmit` exit 0 + `npm run build` clean (14/14, route table intact); anon browser render of `/slot/[id]` confirmed the exact href/target/rel/aria-label + MapPin/ArrowUpRight `stroke=#4A6B8C` + steel venue underline. **e2e FLAG (pre-existing, non-D24):** bare `npx playwright test` (workers:1) = 30 passed / 1 failed / 29 skipped / 4 did-not-run. The 1 red — `dashboard-cancel.spec.ts:242` asserts `/open another slot/i`, but `bfe49e7` (language sweep) renamed the nudge to "Open another game" and never ran e2e; the `describe.serial` block aborts V3–V6 (the "4 did-not-run"). Proven independent of D24 (working tree touches no dashboard file/spec; HEAD already carries the mismatch). No D24 surface has any spec (R8) → D24 added ZERO test regressions. Repair = a separate one-line spec follow-up (`slot`→`game` at line 242). Diff → `~/squadup/exports/d24-full.diff`. NOT pushed.

- **Language honesty sweep + public-court honesty line — BUILT & VERIFIED locally, COMMITTED at HARD STOP. NOT pushed — Ebuka pushes. Diff read + cleared by Architect.** UI-TEXT ONLY, no schema/RPC/route change (branch `feat/language-honesty-sweep`). **Task A — retired user-facing "slot" → game** across three surfaces, 10 rendered strings, each swap preserving the replaced word's case, both "squad" taglines KEPT: `landing-content.ts` ×4 (MANIFESTO "Join a game", STEPS body "Browse open games", CLOSING "Find a game", GAMES_EMPTY_COPY "new games post"), `PublicLanding.tsx:186` ("Pick a game, verify…"), `create-slot/page.tsx` ×3 ("Game created" h1 + "Create Game" h1 + submit button), `dashboard/DashboardClient.tsx` ×2 ("New game" NewSlotButton, "Open another game" waitlist nudge). **Task B — new honesty subline** on `/slot/[id]` directly under the venue card: "Free public court — walk-up, park rules apply." — a `<p className="mt-2 mx-6 text-[#5E80A3] text-[13px]">` + nunito var, REUSING the file's existing steel hex `#5E80A3` (same value as the neighbouring venue-neighborhood + smsCopy sublines; that page predates tokenization and uses raw hexes throughout) — NO hand-rolled color, so it rides the iceboxed steel→steel-aa AA-contrast migration for free. **GUARD (fence held):** every other "slot" in `src/` is an identifier and stayed untouched — `slots` table, RPCs (`join_slot`/`cancel_slot`/`leave_slot`/`slot_share_preview`/`slot_roster`/`slot_wall`), routes (`/create-slot`, `/slot/[id]`), component/type/var names (`SlotCard`/`PublicSlotCard`/`OwnerSlot`/`PhotoSlot`/`slotId`), `data-slot-id`/`data-testid`, hidden `<input name="slotId">`, URL query keys (`?slot=` HomeClient contract + `?slotId=`), server logs, mock/seed IDs. No un-enumerated rendered "slot" surfaced → nothing auto-fixed beyond the list. **VERIFIED (raw):** post-edit grep — zero rendered "slot"/"Slot" survive (deterministic grep + a read-only 5-agent adversarial sweep over src + supabase migrations + sms transport → verdict CLEAN, 0 survivors); `npm run build` EXIT 0 (Compiled successfully, tsc clean, 14/14 pages, route table intact); diff → `~/squadup/exports/language-honesty.diff` (`git diff --stat`: 5 files, +18/−10). NOT pushed.

- **Lobby invite: share button + QR — v1's final feature. BUILT & LIVE-VERIFIED @390 locally. NOT pushed — HARD STOP (Ebuka drags the diff; QR-scan gate at his review). [LEDGER DRAFT — flagged for wording review]** An "Invite a friend" section in the group lobby (BELOW the roster, ABOVE Game chat — Ruling 3) so every game has a sender. ONE commit, NO schema/RPC; the receiving end (`/slot` + `slot_share_preview` unfurl) is untouched. **QR dep (Ruling 1):** `uqr@0.1.3` added as a runtime dep after a survey — ZERO transitive deps and the smallest zero-dep QR lib (79 KB unpacked vs qrcode-svg 248 KB / @paulmillr/qr 343 KB / qrcode-generator 555 KB; `qrcode` pulls a transitive stack). Server-side only (`src/lib/qr.ts` + `import 'server-only'`); the SVG string is passed to the island as a prop, so uqr never reaches the browser bundle. The ~10 KB gate was the Architect's runtime miscalibration (the 79 KB is types/source-maps/dual-format disk inflation) — accepted. **Server (`group-lobby/page.tsx`):** composes the absolute slot URL (`${NEXT_PUBLIC_BASE_URL}/slot/<id>`), the QR SVG (ink `#14304D` modules on white + 4-module quiet zone), the MEMBER-FACING subtitle (RAW count: "{N} spots left · link or QR, no app needed" / "Game locked at {cap} · share for next time"), and the MASKED voucher share text (`formatCentral` per Ruling 2 + `spotsClause`). Eligibility gate written explicitly (`viewer === 'owner' || self !== null`) though currently always true — self-documenting, guards future lobby-access changes. **`src/lib/spots-clause.ts`:** the SINGLE outbound threshold helper, mirroring the SQL `>= 0.5` rule exactly (comment cross-refs `slot_share_preview` / `get_public_feed`) — clause only when ≥50% AND not full, else null. **Client island (`InviteControl.tsx`):** sheet (scrim + slide-up, 44px close, LeaveSheet pattern) with the QR as an inert data-URI `<img alt="QR code linking to this game">` (implicit `role=img`; no raw-HTML injection — safer + a11y-named), the link in a copyable pill, and Share (`navigator.share({text,url})`, AbortError = silent no-op) + Copy (`clipboard.writeText` + "Copied"); when share is unavailable Copy is primary and Share hides; SSR-safe feature-detect. `invite-*` testids. Tokens only — the sole hex is the dispatch-specified QR ink. **AMENDMENT CLOSURE (`owner_first_name`):** the `/slot` METADATA is already host-neutral (confirmed); the `/slot` page BODY rendered the organizer avatar-initial + "Organized by {name}" → STRIPPED per the waiver (the "Owner line" block + the `initial` const removed; `owner_first_name` stays in the `SlotPreview` type / `slot_share_preview` projection — server-only, is_host pattern). The quarantined `opengraph-image.tsx` has no owner name and was NOT touched (its redesign is the next dispatch). **VERIFIED (raw):** `tsc --noEmit` EXIT 0; `npm run build` EXIT 0 (route table intact). **MASK GATE** (`e2e/share-invite.spec.ts` — stubs `navigator.share`, fires Invite→Share at three fills): 2/6 (33% < 50%) → NO fill-count clause; 3/6 (= 50%) → "3 spots left"; 6/6 (full) → NO fill-count clause (date/time digits legitimately remain, so the gate asserts the fill-count clause specifically). Full battery bare `npx playwright test` → **35 passed / 29 skipped / 0 failed** (34 prior + the new chromium mask test; lobby-wall/player-leave/dashboard-cancel green — no assertion rewrites; the invite uses `invite-*` testids and never restructures the roster/wall). **Device @390 (prod `next start`, in-app browser):** OWNER + joined MEMBER both see the section (below roster / above Game chat); sheet opens with the QR (alt confirmed) + the `/slot` link pill; Copy passed the correct `/slot` URL and flipped to "Copied"; this browser exposes no `navigator.share` → Copy primary + Share hidden (the specced fallback; the masked share path is covered by the e2e). Device fixtures seeded + torn down; `seed.sql` + cloud untouched. **MANUAL GATE for Ebuka at diff review: scan the QR → lands on the game's `/slot` public page.** NOT pushed.

- **Dashboard exits + owner join-from-dashboard — BUILT & LIVE-VERIFIED @390 locally. NOT pushed — HARD STOP (architect drags the diff). [LEDGER DRAFT — flagged for wording review]** Fixes the dashboard dead-end and lets the owner join their own game from it. Three changes, ONE commit, NO schema/RPC (`join_slot` is owner-agnostic — verified live). **(A)** `BottomTabBar` now renders on `/dashboard`: `activeTab` made optional (`activeTab?: TabKey`) so the bar shows with nothing highlighted (the dashboard is reached via Profile, not a tab); Home + Profile links live, 44px preserved, and the pre-existing `pb-24` on the dashboard `<main>` already reserved the space. **(B)** Each card gains an action row — a coral min-44px "Join this game" (`data-testid="join-from-dashboard-<id>"`) LEFT of "Cancel game", shown ONLY when the owner is not active AND the slot isn't full (**Fork 2** — an owner never waitlists their own game; gate `!active && !full`). It reuses `joinSlotAction` UNCHANGED (no forked join): a `joined` outcome server-redirects to the lobby ("the lobby is the confirmation", **Fork 1**); waitlisted/error stay on the dashboard with HomeClient's **verbatim** toast copy + `router.refresh()`. When the owner IS active → a steel "In lobby →" link replaces the button; on a full slot they're not in → "View lobby →". `dashboard/page.tsx` gained ONE read — the owner's active membership per slot (`session_memberships`, `user_id = current_user_id()`, `slot_id IN […]`, status joined/waitlisted), mirroring the lobby's self-membership query; **no schema change**. **(C)** Every card's venue name links to `/group-lobby?slotId=<id>` — a universal lobby tap target distinct from Join/Cancel (recon found no prior lobby link). **VERIFIED (raw):** `tsc --noEmit` EXIT 0; `npm run build` EXIT 0 (route table intact, `/dashboard` ƒ). Full battery bare `npx playwright test` → **34 passed / 28 skipped / 0 failed** — dashboard-cancel V2–V6 green with NO assertion rewrites ("Join this game" ≠ `/join game/i`, existing testids untouched). **Device @390 (prod `next start`, in-app browser, owner via /auth OTP +15555550101):** dashboard shows the tab bar; a not-full slot shows "Join this game"; tapping it ticked 0/6→1/6 and server-redirected to that game's lobby with "Dev Owner · that's you" on the roster; back on the dashboard the row read "1/6 joined · In lobby →"; a second **same-date** slot's Join surfaced the D9 toast **verbatim** ("You're already in a game today. Leave that one or complete it to join another.") with NO nav (path stayed `/dashboard`); a **6/6 slot showed NO Join button — only "View lobby →"** (Fork 2 proven on both a demo-seed full slot and a seeded one); Home tab → `/`, Profile tab → `/profile`. Device fixture (empty A + same-date B + full C, owner-owned) seeded + torn down locally; `seed.sql` untouched, cloud untouched. NOT pushed.

- **e2e repair + test-infra hardening — battery fully GREEN and rerun-stable on the bare invocation. NOT pushed — HARD STOP (architect reviews the diff). [LEDGER DRAFT — flagged for architect wording review]** ONE commit, TEST-INFRA ONLY (zero `src/app`/component edits). **What changed:** (1) **`workers: 1` pinned unconditionally** in `playwright.config.ts` — the live-session specs share one mutable local DB with overlapping throwaway phones (+15555550102 / +15555550199 / +15555550198), so parallel FILES collided on `users_phone_key`; serial makes the bare `npx playwright test` THE documented invocation (per-spec RUN comments updated to match). (2) **home-feed `:169` inverted** — the stale pre-D20 "anon / → /auth" assertion became "anon / renders the D22 landing" (manifesto heading "Join a squad, without the group chat." + the "This week in Dallas" section present; URL stays `/`, no redirect); its 4 downstream serial tests unblocked. The one downstream that could have surprised — anon `/onboarding` → `/auth` — is UNCHANGED canon (D22 only touched `/`) and passes as-is. (3) **dashboard-cancel fixture made self-contained** — it referenced 14 filler tester phones in the membership seed but never inserted them as `public.users`, so the seat JOIN silently dropped them and every count came out 0 (the "2/0 vs 0/0" V2 drift); the fixture now seeds the 14 fillers as `public.users` rows (shaped like seed.sql, NO auth binds — pure roster fillers joined via `users.phone`) and tears them down (FK order: memberships → slots → users). `supabase/seed.sql` is UNTOUCHED (D20 demo baseline stays canonical). (4) **auth.users teardown hardening (all 4 fixtured specs)** — every `teardownSql` now also deletes the throwaway `auth.users` rows its dev-logins minted (phone stored WITHOUT the leading '+') with a HARD-CODED guard `AND phone <> '15555550101'` so the dev owner can never be in the deletion set; this closes the banked gap that broke reruns. `twilio-consent` has no DB fixture → untouched. (5) **Housekeeping** — `.gitignore` gains `exports/` + `screenshots/` (both now ignored; `files/` and "untitled folder/" deliberately LEFT un-ignored for Ebuka's manual triage); `~/squadup/exports/` created (STANDING RULE — doc exports never go to Desktop). **VERIFICATION (raw):** local `supabase db reset` → bare `npx playwright test` → **34 passed / 28 skipped / 0 failed** (28 skipped = the webkit-iphone variants of the chromium-guarded live specs; 34 = chromium live [dashboard V2–V6, home-feed ×5, lobby-wall ×7, player-leave ×11] + twilio-consent ×3 on both projects). Then the RERUN-STABILITY gate: bare battery AGAIN with NO reset → **34 passed / 28 skipped / 0 failed** — the point of the whole dispatch, proving the teardown fix; `auth.users` held at **1 row = the dev owner** across both runs (guard verified intact). `npm run build` EXIT 0 + `tsc --noEmit` clean (tsconfig `**/*.ts` covers the e2e specs). **Docs (same commit):** techContext's two harness gotchas rewritten to their post-fix state (one line of history each); drift-backlog's two reds moved to a new 2026-07-08 Closed subsection with resolutions. **Diff exported** to `~/squadup/exports/e2e-repair.diff`. NOT pushed.

- **D22 — public landing front door (Option B), BUILT & LIVE-VERIFIED locally. NOT pushed — HARD STOP (architect reviews the diff + 3 shots). [LEDGER DRAFT — flagged for architect wording review]** Anonymous `/` now renders a marketing landing (`src/app/PublicLanding.tsx`, server component) that wraps the live public feed as its "This week in Dallas" section; authed routing is byte-for-byte UNCHANGED (session → `HomeClient`; the only `page.tsx` edit is the anon branch `<PublicFeed/>` → `<PublicLanding/>` + import + comment). **Extraction (architect ruling R1):** the old standalone `PublicFeed.tsx` shell was DELETED and its feed pathway extracted to `src/components/PublicFeedList.tsx` (async server RPC fetch + `PublicSlotCard` shared-by-import, D20 isolation intact; empty-state copy is a PROP — the landing passes the D22 string). `PublicSlotCard`'s stale comment re-pointed to `PublicFeedList`. **Tokens (R4):** `globals.css` gained EXACTLY two — `--color-steel-aa #4A6B8C` (net-new WCAG-AA body-size secondary text; steel `#5E80A3` stays for large/decorative) and `--color-coral-dark` RETARGETED `#EE5E00`→`#D95500` (0 live usages — cards hover via `brightness` util — so the semantic name got its intended darker value; the RETARGET branch of R4 fired, not add-`cta-hover`). New TSX uses tokens only (zero raw hex). **Copy + manifest (one owner-editable place each):** `landing-content.ts` (ROTATOR 2 phrases + constant coral "your squad." tail, STATS 12·3·6, MANIFESTO, STEPS, FAQ, TESTIMONIALS honest-placeholder-only, CLOSING) and `landing-photos.ts` (`LANDING_PHOTOS` hero/pol1-3/step1-3, **ALL `src: null` at launch** → labeled dashed placeholders using the D22 alt shot-list). **Island scope:** ONE client island (`LandingMotion.tsx`) that ONLY animates server-rendered finals — rotator swap, IO section fade-ups, stats count-up, smooth anchor scroll — by adding `.landing-js` to `#landing-root`; a component-scoped `<style>` (NOT globals.css) holds the gated CSS + keyframes + polaroid sway + hero Ken Burns. First rotator phrase + final stat numbers + all copy render with NO JS. `prefers-reduced-motion` → island returns early (no `.landing-js`): static first phrase, no count-up, no sway, no Ken Burns, native anchors. **VERIFIED:** `npm run build` EXIT 0 + `tsc --noEmit` clean (route table intact, `/` ƒ dynamic); anon `curl` of the PROD build literally contains the manifesto line, `>12<` + "players this week", `data-final="12"`, the testimonial placeholder line, and the first rotator phrase (server-rendered proof) with the old "Upcoming pickleball games" h1 GONE; gates — `grep "PublicFeed\b" src/` 0 (R6), raw `#4A6B8C`/`#D95500` outside globals.css 0, no new `unpkg`/`googleapis`/`cdn` refs. **Device @390 (prod stack, Playwright), 3 shots in `./screenshots/`:** `d22-landing-anon-full.png` (all 12 sections, dashed placeholders, real Churchill card in the games section, `.landing-js` active + rotator cycling), `d22-landing-anon-reduced-motion.png` (emulated `reduce`: no `.landing-js`, rotator frozen on the first phrase, 0 reveals hidden, stats static 12·3·6), `d22-authed-home-unchanged.png` (real `/auth` OTP as Ada → `HomeClient` renders greeting + untouched HeroText + SlotCard + Home/Profile tabs — the anon landing never shows for a session). **e2e — GREEN** (after the architect-authorized local `supabase db reset` — the documented verify base; cloud untouched, no `--linked`). `player-leave` **11/11** + `lobby-wall` **7/7** pass on chromium via `--workers=1` (the CI/serial invocation): the two specs both seed tester phone `+15555550102`, so the local default parallel workers collide on the shared DB — serial is the documented run. D22 has ZERO intersection with those surfaces regardless (`/group-lobby`/`actions`/`LeaveSheet`/`LobbyWall`/`dev-*`/`session_memberships`/`chat_messages`). `home-feed` + `dashboard-cancel` stay known-red pre-existing (untouched); the `home-feed:169` fix clause was re-aimed at the D22 landing in `drift-backlog.md`. **Docs:** decision doc `D22` + the drift-backlog AA-gap entry ride Commit 1; the two commits are staged only after the architect confirms the single `landing.diff`. NOT pushed.

- **Card visual cleanup — Phase 3 (Commit 4): neighborhood on public slot cards, on `main`. NOT pushed — dispatch PARKED (final commit; architect reviews all shots).** Wire the `get_public_feed` `neighborhood` column (already in the RPC projection + `PublicFeedRow` type, previously unconsumed) through to the card. `PublicSlotCard` gains a `neighborhood: string | null` prop (typed nullable to match the RPC honestly — minor deviation from the dispatch's literal `string`, avoids a forced coalesce); Row 3 now renders `"{venueName} · {neighborhood}"` with the venue at `font-medium` and the neighborhood a lighter `font-normal` suffix (the §B "lighter weight" — both stay `text-ink-soft`, no new low-contrast token; suffix omitted when null). `PublicFeed.tsx` passes `neighborhood={row.neighborhood}` + de-staled its "does not consume it" comment. **SlotCard + the authed feed are UNTOUCHED** (no neighborhood there — confirmed: empty working-tree diff for SlotCard.tsx in this phase). **VERIFIED:** `tsc --noEmit` exit 0; `npm run build` exit 0. Local seed neighborhoods confirmed via `get_public_feed()` (Cole Park·Lakewood, Churchill Park·Preston Hollow, Lake Highlands North Park·Lake Highlands). **Prod-stack Playwright @390×844, logged-out `/`:** all 5 cards read "Venue · Neighborhood" (`screenshots/phase3-neighborhood-public.png`); the long Lake Highlands row wraps gracefully inside the inset. **3-commit card-visual dispatch COMPLETE (007be14 hero font + Commit 2 tabular-nums rider + Commit 3 44px CTA + Commit 4 neighborhood). NOT pushed — parked for architect review.**

- **Card visual cleanup — Phase 2 (Commit 3): CTA min tap target 44px (WCAG, amendments §D), on `main`. NOT pushed.** Bump the JOIN CTAs to a ≥44px floor via `min-h-[44px]` + `flex items-center justify-center` (keeps `py-2.5`'s visual weight, just guarantees the WCAG target + vertically centers the label in the taller box — the pattern PublicSlotCard's CTA already used). Edited: `SlotCard.tsx` "Join game" (232) + "Join waitlist" (223) + the in-flight "Joining…" pending button (211); `PublicSlotCard.tsx` "Join this game" (138, already flex → just `min-h-[44px]`). **The FOUR terminal/membership footers (cancelled 175, started 182, in-lobby 191, on-waitlist 201) are UNTOUCHED** (architect scope: not join CTAs, no D19-visual risk). **JUDGMENT CALL (flagged, one-line revert):** the pending "Joining…" button was bumped too though it's not one of the three named CTAs — it's the in-flight state of "Join game"/"Join waitlist", so leaving it at ~38px would make the button visibly shrink 6px on tap (a regression the 44px change itself introduces); it's not a terminal/membership footer and carries no D19 risk. **VERIFIED:** `tsc --noEmit` exit 0; `npm run build` exit 0. **Prod-stack Playwright @390×844 (innerWidth=390 confirmed), bounding-box measured:** every Join CTA = **44.0px** (w=301) — public "Join this game" ×5; authed "Join game" ×4 + "Join waitlist" ×1. Hero `font-variant-numeric: tabular-nums` re-confirmed on both feeds (Commit-2 rider proof). Shots: `screenshots/phase2-cta-44px-public.png` (logged-out) + `phase2-cta-44px-authed.png` (HomeClient) — taller CTA reads substantial, not chunky. **NEXT: Phase 3 — neighborhood prop on PublicSlotCard.**

- **Card visual cleanup — rider (Commit 2): `tabular-nums` on the hero day/time, on `main`. NOT pushed.** Architect KEPT Baloo 2 (007be14 stands, not reverted) and added a pure-upside rider: append Tailwind's `tabular-nums` (`font-variant-numeric: tabular-nums`) to the hero day·time `<p>` in BOTH `SlotCard.tsx:149` AND `PublicSlotCard.tsx:98`, so the time stops shifting width as digit counts change (1:24 → 11:00). Hero-only — the optional fill-count `tabular-nums` was left out to keep every SlotCard change architect-approved + the diff minimal (one-line follow-up if wanted). `tsc --noEmit` exit 0; computed `font-variant-numeric: tabular-nums` confirmed in the Phase-2 Playwright run (same tree).

- **Card visual cleanup — Phase 1 (Commit 1): hero day/time → `font-serif` (Baloo 2), BUILT & LIVE-VERIFIED locally on `main`. NOT pushed — ⛔ ARCHITECT APPROVAL GATE.** First of a 3-commit card-visual dispatch (drift items A/B/C logged during the public-feed build). Commit 1 swaps the hero day·time row from `font-sans` (Nunito) → `font-serif` (Baloo 2, `--font-baloo2` via the `@theme` token at `globals.css:12`) to match the typography + slot-card specs. **PREMISE CORRECTION (Phase-0 flag, architect-confirmed):** the dispatch assumed "SlotCard fixes propagate to both feeds by shared reference" — FALSE. Only `skillBadge`/`SkillLevel` are shared imports; the hero (and CTA) classNames are **duplicated string literals**, identical by RULING #2 discipline, NOT a shared constant. So the swap was applied **independently in BOTH** `src/components/SlotCard.tsx:149` AND `src/components/PublicSlotCard.tsx:98` (the public card's now-stale "SlotCard ships font-sans" comment was corrected to a duplication-with-discipline note). No shared constant extracted — that would couple the public surface to the D19-hardened SlotCard, against the D20/RULING #2-#3 isolation boundary. **SlotCard JSX blast radius = HomeClient (authed feed) only** (verified: the default `SlotCard` import is HomeClient's alone; the 4 other importers take only the named exports). **VERIFIED:** `tsc --noEmit` exit 0; `npm run build` exit 0 (route table intact, `/` ƒ dynamic). **Prod-stack Playwright @390×844, 2 shots in `./screenshots/`:** H1 `phase1-h1-public-baloo2.png` — logged-out `/` (PublicFeed; cleared the persisted session for a true anon load) with 5 PublicSlotCards, hero computed `font-family:"Baloo 2"` @600, "Join this game"/"phone sign-in required"/"Log in" present, NO BottomTabBar; H2 `phase1-h2-authed-baloo2.png` — authed `/` via the real /auth OTP (+15555550101/123456 → Flow-1 onboarding "Ada") = HomeClient still renders 5 SlotCards (4× "Join game" + 1× "Join waitlist" = the 50% spread, greeting, Dashboard tab) with the same Baloo 2 hero — **the D19 authed-feed regression check passes.** **⛔ HARD STOP #1: architect reviews H1/H2 to KEEP Baloo 2 (→ Phase 2) or REVERT (Nunito reads better → git revert → Phase 2 on the reverted base). Phases 2 (44px CTA, both files) + 3 (neighborhood on PublicSlotCard) NOT started.** NOT pushed.

- **/auth enrichment — Flow-2 context banner + Flow-1 strip + trust lines, BUILT & LIVE-VERIFIED locally on `main`. NOT pushed — HARD STOP (architect reviews 4 screenshots).** Makes `/auth` two-variant by entry path (D2 Flow-1 cold vs Flow-2 slot-link) per AMENDMENTS §C, so the public feed's "Join this game" → `/auth?slotId=` keeps the game visible through sign-in. **`src/app/auth/page.tsx`** (Server Component) — reuses the EXISTING anon `slot_share_preview` fetch (widened the local type to skill_level / gender_category / capacity / fill_count / fill_ratio_shown / is_cancelled; swapped the inline `formatBanner` → `formatCentral`). **Flow-2 `JoinBanner`** (shown on BOTH steps, ruling 1): paddle icon + coral "you're joining" eyebrow + Women's/Men's tag for gendered slots (ruling 4) + `font-serif` "{day} · {time}" hero + "{venue} · {skill}{fill}". **Fill clause — three states, full-FIRST precedence:** full (`fill_count === capacity`) → "you're joining the waitlist" eyebrow + "join the waitlist for this game" header + NO count; ≥50% (`fill_ratio_shown`) → " · N spot(s) left" (pluralized); masked (<50%, NULL) → venue·skill only — never "0"/"undefined spots left". A **cancelled** slot (`is_cancelled`) falls back to the Flow-1 cold variant (ruling 2). **Flow-1 cold** → slim 3-step "how it works" strip (find a game · join six players · show up & play) + "Enter your number to log in or join a game". **Lowercase `sonye` wordmark on both variants** (ruling 3, replaces the capitalized h1). **`src/app/auth/PhoneForm.tsx`** — added the trust line "No password. We'll text you a 6-digit code." ABOVE the consent block + the reassurance "Your number stays private and is never shared." below the Send-code button; migrated the CTA to the `bg-coral` token. **The SMS consent disclosure (`SMS_CONSENT_LINE`) is BYTE-IDENTICAL and in position — `consent.ts` untouched, the `data-testid="sms-consent"` block unchanged.** **VERIFIED:** `tsc --noEmit` exit 0; `npm run build` exit 0 (`/auth` ƒ dynamic); `consent.ts` not in the diff. **4 Playwright shots (390×844, prod stack) in `./screenshots/auth-0*.png`:** (1) Flow-2 counted+gendered (d4 3/6 women's → "3 spots left" + Women's); (2) Flow-2 masked (d1 1/6 → no count clause); (3) Flow-1 cold (strip + "log in or join a game"); (4) Flow-2 full→waitlist (d3 6/6 → "you're joining the waitlist", no "0 left"). Consent + phone field present/unaltered in all four. **NOT pushed.**

- **Public feed UI — Phase 2: routing split + `PublicFeed`, BUILT & LIVE-VERIFIED locally on `main`. NOT pushed — HARD STOP (architect reviews the 4 screenshots first).** Wires the logged-out front door at `/` (D20 + amendments §B) on top of the `get_public_feed` RPC + `PublicSlotCard` (6a302ef) + the demo seed (326117e: 5 slots, member_count 1/4/6/3/2). **Net-new `src/app/PublicFeed.tsx`** — async Server Component (NO 'use client'): fetches via `createAnonClient().rpc('get_public_feed')` (the anon-surface convention — logged-out, no session; an RPC error falls back to the empty state, never a blank screen), derives dayLabel/timeLabel SERVER-SIDE via `formatCentral` (RULING #1), maps rows → `<PublicSlotCard>`. Renders the lowercase `sonye` wordmark + `dallas` pill + outlined `Log in`→/auth header, the intro headline + subhead, the 5-card feed (or the calm "No open games right now" empty-state card at 0 rows), and a Privacy·Terms footer. **`src/app/page.tsx` routing split:** the ONE cold-visitor line `if (!user) redirect('/auth')` → `if (!user) return <PublicFeed/>` (+ import; removed the now-dead `redirect` import; corrected the stale anon-handling comment that still described the old login-wall). Authed branch BYTE-FOR-BYTE unchanged; `dynamic='force-dynamic'` kept; no 'use client'. **DEVIATION:** `PublicSlotCard` has no `neighborhood` prop, so the RPC's neighborhood column is not passed (mapped per the card's interface, as the dispatch directed). **VERIFIED:** `npm run build` exit 0 (tsc clean; `/` is ƒ dynamic); greps confirm the cold redirect is gone / no `from('slots')` in PublicFeed / no 'use client' in either file. **LIVE on the prod stack (`build && start`, 390×844) — 4 Playwright shots in `./screenshots/`:** (1) logged-out `/` populated — the 50% mask renders correctly against the seed (d1 1/6 + d5 2/6 button-only; d2 4/6, d3 6/6, d4 3/6 show the dot meter + "X/6 filled · N left"; d4 carries the Women's tag); (2) ≥50% card close-up (d2); (3) empty state (slots transiently shifted past → 0 rows → exact §B copy, restored after); (4) authed `/` via the real /auth OTP (+15555550101/123456; /dev-login is prod-404) → HomeClient still renders the feed (greeting / HeroText / SocialProofStrip / BottomTabBar + "Join game"/"Join waitlist" + "X/6 spots filled") — the routing change did NOT touch the authed path. **NOT pushed.**

- **Public feed UI — Phase 1: `PublicSlotCard` component (D20 surface), BUILT & VERIFIED locally on `main`. NOT pushed / Phase 2 pending — HARD STOP.** The render-only logged-out slot card on top of the D20 `get_public_feed` anon RPC (the UI half of the public-feed dispatch; **SlotCard untouched** per the D20 scope boundary). Net-new `src/components/PublicSlotCard.tsx` (`"use client"` so the NAMED `skillBadge`/`SkillLevel` import from the client SlotCard module resolves to a real object, not an RSC client-reference proxy — RULING #3). **Token discipline (RULING #2, anti-drift):** every visual SHARED with SlotCard uses the SAME Tailwind class BY REFERENCE — container, gender tag, skill badge, hero day·time row (byte-identical to `SlotCard.tsx:149`), venue inset, and the coral CTA base (`bg-coral text-white rounded-xl py-2.5 …`, the exact "Join game" classes) — NO re-hardcoded coral/font hex (prevents the #D4724A/#EE5E00 drift). **Intentional differences (design-folder values):** icon `w-6` (vs w-7, same `#FF6A00`), Row-1 sport label uppercase 11px `#4A6E92` (amendment-D a11y small-text blue), CTA label "Join this game" + a "phone sign-in required" helper, CTA target `/auth?slotId=` (Flow-2 banner — a navigation, NOT a join), and a capacity-meter fill (`#5E80A3` filled / `#C4D5E8` empty ring + "x/n filled · k left", ≥50% ONLY — the 50% rule). dayLabel/timeLabel are PRE-DERIVED server-side (RULING #1: `formatCentral` → "Sat, Jul 18 · 8:00 AM"); the card never formats dates. **VERIFIED:** `tsc --noEmit` exit 0; a 3-lens adversarial review (token-parity / design-fidelity / rulings-compliance) returned PASS on all three — no HIGH/MEDIUM. **OPEN for architect (raised at the hard stop):** (1) hero font — SlotCard ships `font-sans` here, NOT the design-doc Baloo/`font-serif`; matched SlotCard per RULING #2, flagged as pre-existing **SlotCard** drift; (2) the populated-card seed approach for the Phase-2 visual gate (the "seed question" was missing from the dispatch). **A11y note:** CTA `py-2.5` ≈ 38px is under the 44px target — SHARED with SlotCard's button, so a fix straddles the RULING #2 parity / RULING #3 (don't-touch-SlotCard) boundary → architect call. **NEXT (Phase 2):** the `src/app/page.tsx` routing split (`if session → HomeClient else → PublicFeed`) + a net-new `PublicFeed` server component (anon `get_public_feed` fetch + label derivation + header / intro / empty-state) → the Playwright visual gate.

- **Deploy unblock — removed the Vercel-native cron (2026-06-23, branch `feat/twilio-a2p-resubmission`, NOT pushed — HARD STOP).** The Vercel preview build was failing: the **Hobby** plan rejects sub-daily crons, and `vercel.json` declared an hourly `0 * * * *` → `/api/cron/attendance`, which failed the WHOLE deploy (blocking the `/optin` A2P fix). The D11 dispatcher is inactive until post-cutover (Phase 6), so the `"crons"` block is removed now — `vercel.json` reads `{}` (file kept, not deleted). **At go-live the dispatcher will be triggered by a free GitHub Actions scheduled workflow** (GHA supports sub-daily crons on the free tier) that `curl`s the route with the `CRON_SECRET` **bearer** — $0, stays on Vercel Hobby, and **preserves the `ends_at+2h` cadence (which lives in the `claim_attendance_dispatch` RPC window, NOT the cron schedule)**. The route's existing bearer auth (`Authorization: Bearer ${CRON_SECRET}`) is exactly what GHA will authenticate with — no route change needed. **Verified:** `npm run build` exit 0, tsc clean. **TODO (Phase 6 / cutover):** add the GHA workflow (`.github/workflows/`) hitting `/api/cron/attendance` hourly with the `CRON_SECRET` bearer.

- **A2P resubmission (2/2) — public intro line on the `/auth` phone-entry screen (2026-06-23, branch `feat/twilio-a2p-resubmission`, NOT pushed — HARD STOP).** So a cold visitor / carrier reviewer landing on the bare phone prompt sees what Sonye is. Additive intro ABOVE the form in `src/app/auth/page.tsx` (the shell, where the prompt lived — **PhoneForm.tsx untouched**): an h1 "Sonye" wordmark + tagline "Curated recurring pickleball games in Dallas. Enter your number to log in or join a game.", gated on `!bannerText` (share-link visitors already get the "You're joining …" banner for context). **JUDGMENT CALL:** replaced the now-redundant bare `enter your phone number to log in or sign up` helper rather than stacking it under the near-identical tagline CTA — this is NOT the consent disclosure or form logic (the dispatch's "keep as-is" list was specifically consent line + links + Send-code, all in PhoneForm, all byte-exact). One-line revert if Ebuka prefers keeping both. **NO new imports / no new Supabase dependency** → still renders backend-absent. Consent line + Terms/Privacy links + "Send code" button intact and still share the 390×844 frame (button bottom well above the fold → the `twilio-consent.spec.ts` viewport assertion is unaffected). **Verified:** `npm run build` exit 0, tsc clean; live @390 snapshot+screenshot show the intro above the field with all consent elements present; zero console errors.

- **A2P resubmission (1/2) — public `/optin` page that DISPLAYS the consent screenshot (2026-06-23, branch `feat/twilio-a2p-resubmission`, NOT pushed — HARD STOP).** Root cause of the 30909 rejection: `sonye.app/optin.png` served as a DOWNLOAD, so the carrier reviewer never saw the consent screen. Fix: net-new PUBLIC `src/app/optin/page.tsx` (App Router) that renders the EXISTING `public/optin.png` INLINE (reused, not recaptured) — heading "Sonye — SMS Opt-In", context line, a PLAIN `<img src="/optin.png">` (NOT next/image — no loader indirection so it always renders; `max-w-[420px]` phone aspect, centered, eslint-disable for `no-img-element`), the consent text as page copy from `SMS_CONSENT_LINE` (`@/lib/consent` — byte-identical to the screenshot + the live phone screen), Privacy Policy→`/privacy` + Terms of Service→`/terms` links. Container chrome mirrors PolicyDoc (`/privacy`+`/terms`). **ZERO Supabase/auth imports** (only `next`, `next/link`, `@/lib/consent`) → renders fully backend-absent; route prerenders **○ Static**. **Verified:** `npm run build` exit 0, tsc clean, `/optin` in route table; live render @390×844 — `<img>` `complete=true` natural 780×1688 (loads INLINE, not a download), consent `textContent === SMS_CONSENT_LINE`, links `/privacy` `/terms`, zero console errors.

- **Audit quick-win 2 — OG share base URL from env (2026-06-21, branch `fix/audit-quick-wins`, NOT pushed).** From the deployment-readiness audit (Low finding): `generateMetadata` in `src/app/slot/[id]/page.tsx` built the OG image URL from a hardcoded `http://localhost:3000` / `http` header fallback → broken share previews if the host/proto headers are ever absent. Replaced the `headers()`-derived `baseUrl` with `process.env.NEXT_PUBLIC_BASE_URL ?? 'http://localhost:3000'` (dev-only fallback) and removed the now-unused `next/headers` import. **FOLLOW-UP:** register `NEXT_PUBLIC_BASE_URL` (e.g. `https://sonye.app`) in Vercel env + `.env.example` so the fallback never fires in prod. No schema, no behavior change.

- **Audit quick-win 1 — PII phone log redacted (2026-06-21, branch `fix/audit-quick-wins`, NOT pushed).** From the deployment-readiness audit (High finding): `verifyOtpAction` in `src/app/auth/actions.ts` logged the full E.164 phone on every OTP verify (`[D2 router] phone=${phone} …`) → plaintext PII in Vercel runtime logs / drains. Dropped the `phone=` interpolation entirely (not partial/last-4) + added a never-log-PII comment; kept the non-PII routing diagnostic (`isNewUser`/`hasSlot`/`hasClaim`/`flow`). Grep confirms no remaining `console.*` in `src/` interpolates `phone`. No schema, no behavior change.

- **Twilio A2P readiness — SMS consent screen + /privacy + /terms (2026-06-18, branch `feat/twilio-a2p-consent-policy`, brought current with `main` 2026-06-21, NOT pushed — HARD STOP).** Frontend-only, NO schema, independent of the D10-A redaction branch (touches the auth phone-entry screen + two net-new PUBLIC routes, not the lobby/roster — merge order with D10-A doesn't matter). **Commit 1 (consent screen):** net-new `src/lib/consent.ts` exporting `SMS_CONSENT_LINE` — the character-exact 10DLC opt-in string (single source of truth; must stay byte-identical to the Twilio campaign field + the consent screenshot). `src/app/auth/PhoneForm.tsx` renders the consent block directly ABOVE the "Send code" submit action: the consent line (`data-testid="sms-consent"`, Nunito/steel) + a separate line beneath with ink-underlined `Terms`→/terms and `Privacy Policy`→/privacy `next/link`s. Block sits above the fold on 390×844 (consent line + button visible in one frame = the Twilio screenshot). **Commit 2 (policy pages):** net-new `content/privacy.md` + `content/terms.md` (seeded as placeholders; **final legal text published 2026-06-21 — content-only, no code change**), rendered by net-new PUBLIC routes `src/app/privacy/page.tsx` + `src/app/terms/page.tsx` (no auth gate — there is no middleware, so they're reachable unauthenticated for carrier/Twilio review) via a shared `src/components/PolicyDoc.tsx` + `src/lib/content.ts` using `react-markdown` + `remark-gfm` (first-party content). Styled per D8.2 (ink-on-bg, Nunito body, Baloo 2 headings, Sonye wordmark header, comfortable max-width). **VERIFIED on the local PROD stack (`npm run build` + `npm run start`, prod parity per dispatch — NOT dev):** build clean (tsc green; `/privacy` + `/terms` prerender ○ Static); lint unchanged at the 4-problem baseline (zero new); unauthenticated `curl` → `/privacy` `/terms` `/auth` all `200` (no redirect = public). New `e2e/twilio-consent.spec.ts` (chromium @ 390×844, reuses the prod server) **3/3 green:** consent line `textContent` asserted byte-equal to `SMS_CONSENT_LINE`, the "Send code" submit shares the 390×844 frame with the consent block (bottom edge inside the viewport), `/privacy` + `/terms` reachable with NO auth redirect + headings render. Screenshots: `test-results/twilio/auth-consent.png` (the Twilio-screenshot preview), `privacy.png`, `terms.png`. **NOT pushed/deployed (HARD STOP). Next:** Ebuka deploys to sonye.app + submits the 10DLC campaign (credentialed; not in this dispatch).

- **D10-B — lobby wall UI (2026-06-18, branch `feat/d10b-wall`, NOT pushed).** Files added: `src/lib/wall.ts` (3 canned messages — fixed bodies "Here"/"On my way"/"Running ~10 late"), `src/components/LobbyWall.tsx` (client island: message list + canned chips [instant-submit, no Send] + coral composer + per-message "…" action sheet [Report everyone → existing support sms, copy "Sends it to Sonye"; Remove host-only] + closed card), `src/app/dev-wall-post/page.tsx` (DEV-ONLY harness, `notFound()` in prod, invokes the post action directly for the server-reject proof), `e2e/lobby-wall.spec.ts`, `supabase/migrations/20260618160000_slot_wall_reader_d10b.sql`, `supabase/verifications/phase5_proofs_slot_wall_d10b.sql`. Modified: `src/app/actions.ts` (+`postLobbyMessageAction` [empty/too-long/ends_at+2h-closed/RLS-not_joined map] / +`removeLobbyMessageAction` [owner_delete_message, owner-only map]), `src/app/group-lobby/page.tsx` (wall data: `showWall = joined||owner`, `wallClosed` on ends_at+2h, explicit `is_owner()` for host-Remove on an owner who joined, `slot_wall` rpc → server-side avatar [seed = author_id, never sent to client] + "You"/name + Central time; closed → COUNT-only; mounts `<LobbyWall>` below the roster). **Verified live (390×844):** `slot_wall` psql 4 legs (joined J resolves all 3 authors + is_self/is_host; waitlister W=0; owner O reads all, own row host=t/self=t; anon=0 despite EXECUTE) — fixture torn down. e2e `lobby-wall.spec.ts` **7/7 green:** (1) NO WAITLIST LEAK — waitlisted SAM gets neither composer nor list, joined CASEY gets both; (2) CANNED TAP — tap "I'm here" → psql row body "Here", list re-renders, no Send; (3) HOST REMOVE — psql 1→0 + DOM gone, non-host has no Remove; (4a) client closed card on expired slot, composer/taps absent; (4b) `postLobbyMessageAction` direct on expired → raw `{"error":"closed"}`, psql count UNCHANGED; (5) render sanity — list+3 taps+composer+footer, "Robin"/"You"+you-badge resolve, Send bg `rgb(238,94,0)` / chip bg white (coral only on Send), screenshot; (GATE2) OWNER-WITH-SEAT — an owner holding a JOINED seat renders BOTH composer (3 taps + Send) AND Remove (composer keys off `viewer==='joined'`, i.e. joined-membership status, independent of host; Remove keys off `is_owner()` separately). `npm run build` ✓ (tsc clean; `/group-lobby` + `/dev-wall-post` routes present); lint zero-new (3 pre-existing errors: dashboard `Date.now`, two `<a href="/">` back-links incl. group-lobby's own — confirmed via `git show HEAD`). **Pre-merge gates verified (all PASS, no fix needed):** (1) `slot_wall` proconfig `{search_path=""}` + header `SECURITY DEFINER SET search_path = ''` (matches is_joined_member/slot_roster); (2) composer gate keys off joined membership not host — owner-with-seat proof leg added; (3) `/dev-wall-post` guards `process.env.NODE_ENV==='production' → notFound()` (404s in cloud build, /dev-login pattern).

- **M5 (D16) — player-leave flow + dashboard nav rider (2026-06-16, branch `feat/m5-player-leave`, NOT pushed).** UI-only, no schema change. Files added: `src/components/LeaveSheet.tsx`, `src/components/LeaveGameControl.tsx`, `e2e/player-leave.spec.ts`. Modified: `src/app/actions.ts` (+`leaveSlotAction`/`LeaveResult`/`LeaveReasonCode`), `src/app/group-lobby/page.tsx` (leave entry point + reads `member_count`/`waitlist_count` from `slots`), `src/app/page.tsx` (+`is_owner()` fetch), `src/app/HomeClient.tsx` (threads `isOwner`), `src/components/BottomTabBar.tsx` (+owner-only Dashboard link). **Verified on the local live stack:** `npm run build` ✓ (tsc clean, all routes present). New live-session E2E `e2e/player-leave.spec.ts` (dev-login + self-contained DB fixture seeded via `docker exec psql`, torn down; chromium; disjoint slot ids `51160000-*` + throwaway phones `+15555550102/199/198` from the dashboard fixture). **9/9 green:** D1 picker is `role=radiogroup` of 5 `role=radio` chips w/ `aria-checked`, **`aria-pressed` count = 0**; D2 confirm disabled until a reason picked + single-select aria-checked flip (one true at a time); D3 note bg `rgb(255,255,255)`; D4 both consequence variants (`waitlist_count` >0 / =0) + no reschedule; D5 leave link present on a FUTURE joined slot, ABSENT on a PAST one (lobby still renders — no grace); NAV owner sees `/dashboard` entry, player does not; **E2E-A** the D9 chain via player-leave (joined slot A → BLOCKED from same-day B → leave A via LeaveSheet → join B succeeds; BEFORE/AFTER guards; A=`left/schedule_conflict`, B=`joined`); **E2E-B** full-slot leave promotes the waitlisted W (`waitlisted→joined`, member_count stays 6, waitlist 1→0, leaver=`[left,injured,NULL]`); **E2E-C** "Switching to another game"→`found_other_game` + note persists in `leave_reason_note`. Fixture torn down clean (0 leftover slots/users; owner + Bob untouched). **DEFERRED:** waitlist-leave + Home-feed leave entry point.

- **Phase 5 Dispatch 2 — owner dashboard + cancel sheet (2026-06-16).** Files added: `src/app/dashboard/page.tsx`, `src/app/dashboard/DashboardClient.tsx`, `src/components/CancelSheet.tsx`, `src/lib/format-central.ts`, `e2e/dashboard-cancel.spec.ts`. Modified: `src/app/actions.ts` (+`cancelSlotAction`/`CancelResult`), `src/app/HomeClient.tsx` (import shared `formatCentral`), `src/components/SlotCard.tsx` (`export skillBadge` + `SkillLevel`). **Verified on the local live stack:** `npm run build` ✓ (`/dashboard` route present, tsc clean). New **live-session** E2E (first of its kind in this suite — dev-login `/dev-login` + a self-contained DB fixture seeded via `docker exec psql` and torn down; chromium): V2 dashboard fill + waitlist-demand render; V3 dynamic consequence copy (plural+waitlist / plural-no-waitlist / singular), reason-gates-confirm, white note field (`rgb(255,255,255)`), no reschedule; V4 cancel → DB row `["t","Weather — courts flooded","0","0"]` + both memberships `["left","slot_cancelled"]` + card drops; **V5 the D9 chain** (player joined slot A blocked from same-day slot B → owner cancels A via dashboard → player joins B; BEFORE/AFTER guards rule out a false pass); V6 no reschedule. **5/5 green, fixture torn down.** **FLAG (RESOLVED 2026-06-16):** the legacy mock specs `e2e/m2.1-store.spec.ts` + `e2e/m2.2-join.spec.ts` were STALE (target the pre-Phase-3 `?onboarded=1` + availability/venue onboarding flow that D7.4 collapse + Phase 4 closeout removed) — all 12 failed at the `/onboarding` step, PRE-EXISTING (12/12 fail on `main` too) and unrelated to this dispatch. **Both DELETED** (every AC tested removed architecture — Zustand name-threading store, `?onboarded=1` "Jordan" seed, mock optimistic join `?simulateFailure=1` flip/rollback/Retry) and **replaced by `e2e/home-feed.spec.ts`** — a live-session spec (dev-login + self-contained DB fixture, disjoint phones/slot ids from dashboard-cancel so both fixtures run concurrently under `fullyParallel` without racing) covering the surviving Home coverage the deletion would otherwise drop: anon `/` + `/onboarding` → `/auth` wall, server-rendered greeting/onboarding-banner (name set vs blank `first_name`), and the waitlisted-toast outcome (full slot → `join_slot` waitlists → "we'll text you" toast + card flips to "On the waitlist" + DB `waitlisted` row). Full `npx playwright test --project=chromium` suite **10/10 green** (5 home-feed + 5 dashboard-cancel). **NOTE:** stopped a leftover `npm run start` (production) server that was squatting :3000 with a stale route manifest (404'd `/dashboard`); Playwright now boots its own `npm run dev`.

- **Phase 4 Dispatch 2 — Home join wiring (Flow 2′, five outcomes), 2026-06-15.** New `src/app/actions.ts` (`'use server'`) `joinSlotAction(slotId)`: server-side `join_slot` via `@/lib/supabase/server`, catches the RAISE contract and maps it — `'joined'` → `redirect('/group-lobby?slotId=')` (lobby = confirmation, no toast); `'waitlisted'` → returns `{status:'waitlisted'}`; `'D9 violation'` → `{error:'collision'}`; `'is cancelled'` → `{error:'cancelled'}`; `'already active in slot'` → route to lobby (shouldn't reach the feed); `'not authenticated'` → `/auth`; unknown/not-found → `/`. NO client Supabase, NO D9 client short-circuit (D13 — `join_slot` is sole authority). `HomeClient` drives both buttons through one `handleJoin` (useTransition + per-card `pendingId` → disabled "Joining…"; `cancelledIds` set → card locks to a non-interactive greyed "Cancelled"; waitlist success → store toast + `router.refresh()` re-fetches the real membership so the card flips to "On the waitlist"; collision → error toast, button returns pre-tap). `SlotCard` gained `pending`/`cancelled` props with precedence: cancelled-lock → joined → waitlisted → Joining… → full(waitlist) → open(join). Toast tokens reused (error `#D64B4B` / success `#4BAE78` — already in `Toast.tsx`); no token change. **Card date fix:** the Central `DAY_FMT` extended `weekday:'long'` → `weekday:'short', month:'short', day:'numeric'` → "Sat, Jul 18 · 6:00 PM" (DST-correct IANA; the `2026-07-19T01:00Z` slot still reads "Sat, Jul 18 · 8:00 PM"). **Verified e2e via `/dev-login`:** JOINED routes to lobby + the MANDATORY `member_count` == joined-rows psql equality + the card now reads "In lobby"; WAITLISTED success toast + `member_count` UNCHANGED psql + "On the waitlist" card; COLLISION D9 toast (two same-Central-date 07-18 slots); CANCELLED proven at RAISE/component level (full e2e awaits Phase 5 owner-cancel); date "Sat, Jul 18 · 6:00 PM"; toast colors `#D64B4B`/`#4BAE78` at computed-style; 390×844 screenshots. `tsc` clean, `npm run build` ✓. **FLAG:** CANCELLED full e2e blocked until Phase 5 ships owner-cancel (the feed excludes cancelled slots, so one can't be tapped from Home today).

- **Phase 4 Dispatch 1 — DB-backed Home feed (render-only), 2026-06-15.** `page.tsx` → async Server Component (`dynamic='force-dynamic'`): server-resolves the session (anon → `redirect('/auth')`), fetches eligible `slots` (`cancelled_at IS NULL` + `starts_at > now()`, ordered `starts_at ASC` — the real-data equivalent of the mock `sortSlotsForHome`), reads the viewer's active memberships (`.eq('user_id', current_user_id()).is('left_at', null)` — explicit `user_id` filter REQUIRED since the owner satisfies `is_owner()`, else RLS returns everyone's rows), and passes `FeedSlot[]` to `HomeClient`. `HomeClient` renders from props (no store feed), formatting day/time from `starts_at` in `America/Chicago` via `Intl` (DST-correct IANA; bare-weekday shape matched to the card). `SlotCard` button area → four locked states (D13): no-membership+open → coral `#EE5E00` "Join game" (stub); no-membership+full → sky `#8DBCF1`/ink `#14304D` "Join waitlist" (stub); active joined → outlined-ink "In lobby" (→ lobby); active waitlisted → muted `#EEF2F8`/`#5E80A3` "On the waitlist" (→ lobby). Neutral D8.1 fill dots from `member_count`; skill badge from `slots.skill_level`; 50% rule fed real data. NO client-side Supabase (server-only fetch). Added a non-visual `data-slot-id` to the card root for the UUID proof. New doc `D8_2-action-colors.md`. **Verified e2e via `/dev-login`:** feed renders 9 real DB UUIDs in `starts_at` order (not `slot_a..slot_d`); Join/Join-waitlist colors confirmed at computed-style level (sky `rgb(141,188,241)`/ink `rgb(20,48,77)`, coral `rgb(238,94,0)`/white); In-lobby/On-waitlist proven at component level (owner holds no memberships); anon redirect; 50% both levels; DST boundary (slot at `2026-07-19 01:00Z` → 'Saturday · 8:00 PM' Central). `tsc` clean, `npm run build` ✓. Render-only — join is Dispatch 2. **FLAGS:** (1) all seeded slots are Saturdays → bare-weekday labels read identically ('Saturday · 6:00 PM' ×6); recommend adding the date (e.g. via the existing `formatDallas`). (2) Greeting + onboarding-banner still read the mock store `currentUser` (generic greeting + banner show for the authed owner) — out of feed-source scope; flag for follow-up.

- **D13 recorded (2026-06-14) — Phase 4 player-surface data architecture (all-server).** New decision doc `memory-bank/decisions/D13-phase4-player-surface-data-architecture.md` (number confirmed free; the doc's "D12 = v2 public-browse" numbering note corrected — real D12 is the `slots.skill_level` patch, public-browse is unnumbered in `v2-signals.md`). Amends D9 (client-side same-day short-circuit dropped → `join_slot` RAISE is the sole guard) and touches D2 Amendment C (client-side-reads premise orphaned; cookie retained, not re-litigated) — breadcrumbs wired into both docs. Every Context/DB claim adversarially re-verified against the live codebase (mock feed, zero client Supabase, server-only `join_slot`, RLS supports the authed feed, `slots.starts_at`/`skill_level` present, `member_count` trigger predicate). Decision doc only — no `src/` changes; pending Ebuka's commit. Dispatch 1/2 not yet authored (Dispatch 1 gated by the architect's sketch + waitlist-copy lock).

- **STAGED, UNCOMMITTED — `/dev-login` bind fix (2026-06-14; one-commit dev-tooling change).**
  The dev shortcut now produces a real RLS-valid session: after `verifyOtp` it binds the seeded
  owner's `auth_user_id` via `signup_claim` Path A (`p_claim_token` omitted → match on phone +
  `auth_user_id IS NULL`), mirroring `submitOnboardingAction`; idempotent (2nd run = Path C,
  read-only). Route gated dev-only — `notFound()` in prod at the page AND both server actions
  (defense in depth; `next build` prerenders it as 404). Verified end-to-end through the route:
  owner `auth_user_id` NULL → `2c8b35af-…` (the GoTrue uid); live `/group-lobby?slotId=6e555c6c…`
  now RENDERS (was redirecting to `/slot/{id}`) with the live `getAvatar` mapping (Maya woman→pink,
  Theo man→blue, Ray/Jess/Bob null→blue). **Local dev owner can now load RLS-gated surfaces —
  Phase 4 local testing unblocked.** tsc + build clean. (Tree also still carries the uncommitted
  D8.1 batch below — commit that separately.)

- **STAGED, UNCOMMITTED (2026-06-14 — pending Ebuka's review; commits as DOCS then IMPL+activeContext).**
  - **D8.1 avatar swap — LANDED.** `src/lib/avatar.ts`: `WOMAN_FAMILY` finalized to the four
    pinks (#F18EB7/#A25D7A/#4D142C/#D29DB3, fg by the blue contrast rule); `NEUTRAL_FAMILY`
    renamed `NONBINARY_FAMILY` (green #246B42, NB-only); `getAvatar` routing now woman→pink,
    non_binary→green, everything else (man/PNTS/null)→blue (default). MAN_FAMILY blues + the
    green hex unchanged; shade-hash + signature untouched.
  - **Home fill dots neutralized (D7.3 enforced).** `HomeClient.tsx` no longer derives per-user
    color (dropped `getAvatarColor` import + calls; fill row keeps id/count only); `SlotCard.tsx`
    SocialProofBlock renders every dot in a single neutral `#AEBED0` (`FILL_DOT_COLOR`). Dead
    `getAvatarColor` removed from `mockData.ts` (0 importers); `User.avatar_color` seed field
    retained (seed integrity), comment de-staled. Gender→color is now lobby-only.
  - **Owner avatar unchanged** — it's a text initial (`slot/[id]/page.tsx:99,201`), not a
    gender-colored avatar; nothing to swap.
  - **Icon svgo (prior overnight Task A) still staged** — `PickleballIcon.tsx` optimized
    (5675→4276 B), renders pixel-identical.
  - **Docs reconciled to the corrected palette:** D8.1 + D7.3 + new `D8_1a-avatar-null-blue.md`
    (pink now #F18EB7…, green #246B42, blue D8.2 set #8DBCF1…, NOT the earlier stale
    #511A31/#1A512C); D7.3 + D8.1 record Home-neutral enforcement; `CLAUDE.md` avatar blue
    updated to the D8.2 set. §5 (opengraph-image, slot-preview) untouched — OG is a separate dispatch.

- D8.3 shipped: slot-card sport icon = bare orange paddle (PickleballIcon,
  currentColor, #FF6A00) + "Pickleball" label, replacing Activity + coral chip.
  Detail-page "spots" pluralization fixed (lines 44, 284). Join-button recolor +
  Home re-skin still pending.

- **M4 Phase 4B — D11 attendance confirmation routes + peer-report link + D9 toast repair.** Landmine (Option A, no schema change): attendance routes are tapped by ANON (token is the auth), but `attest_attendance` is granted `authenticated`-only → new server-only `src/lib/supabase/service.ts` (SUPABASE_SERVICE_ROLE_KEY, `persistSession:false`) satisfies the grant; imported ONLY by the two `/c` routes, never a client component. Two thin Server Components `src/app/c/y/[token]/page.tsx` + `src/app/c/n/[token]/page.tsx` call `attest_attendance(token, true|false)` and render shared `src/components/AttendanceResult.tsx` off the status string (success: Baloo "got it" + Nunito subline "glad you made it out" / "thanks for letting us know"; invalid/expired: "this link's expired" + peer link). `src/components/PeerReportLink.tsx` (muted steel, single source) on the lobby (all viewer states, venue+day in sms body) and the invalid page (generic body — no membership in scope); reads `NEXT_PUBLIC_SUPPORT_PHONE` directly, omits the link if unset (never `sms:undefined`). D9 toast: HomeClient copy → locked canon "You're already in a game today. Leave that one or complete it to join another."; Toast.tsx re-anchored top→bottom (`bottom: calc(safe-area+16px)`, slide flipped to `translate-y-full`) for the scroll-to-see fix. New tracked `.env.example` (placeholder inventory; no real secrets). Battery green: tsc/build ✓, D11 token lifecycle 4 legs (success+atomic-invalidate, re-tap invalid, opposite-path no-op, expired≠consumed) with before/after psql, member_count 4 legs (join +1, full→waitlist, status→left trigger-reconcile, CHECK backstop reject; DELETE leg dropped per G2), D9 no-mutation proof (locked toast verbatim + identical snapshot), Flow 2′ trace (`/auth→/auth?step=otp→/join→/group-lobby`, zero onboarding), peer-link grep (real placeholder, not undefined), 390×844 screenshots passed Ebuka's visual review. NO schema change → single impl commit.

- **M4 Phase 4A — group lobby (D10 read-render) + join wiring.** Schema micro-dispatch 4A-pre first (isolated commit): `slot_roster` gained `ORDER BY (status='joined') DESC, created_at ASC, id ASC` (joined block first, FIFO — waitlist ordinals depend on it); ordering proof ×2 + d10 proofs 9–11 re-run green; **cloud `db push` carried m3_4 + m3_5 + jwt-fix along (cloud had never received them — verified benign: tables empty, history now current with local).** Then: `/group-lobby` rewritten as a dynamic Server Component (no Suspense) — redirect ladder (no slotId → `/`; no session → `/auth?slotId=`; bad slot → `/`; zero roster rows → `/slot/{id}`), G2 self-pin via `rpc('current_user_id')` + explicit membership filter, three viewer states (joined / waitlisted / owner-not-member = joined layout minus pill/self/banner). D10 UI double-gate on digits; sms links `(XXX) XXX-XXXX` + locked prefill body; locked copy throughout (pills, banner, ordinals, chips, dir-help). New utils: `src/lib/avatar.ts` (D8.1 families, membership-id hash shade), `src/lib/phone.ts`, shared `src/components/JoinDisclosure.tsx` (locked disclosure at slot/[id] CTA + onboarding slot-flow; SlotCard banked until home wires real joins). Join outcomes joined/waitlisted now redirect to `/group-lobby?slotId=` (actions.ts + join/page.tsx; dead slotDesc toast fetches deleted; welcomed/cancelled/d9 unchanged). Five zero-importer stubs deleted (GroupLobbyClient, LobbyHeader, CommitmentTracker, MemberAvatarStack, ChatMessageList) — no chat surface anywhere. Lobby `SKILL_RAMP` is the CANONICAL D8.2 ramp (beginner `#DCEBFF/#15457B`, adv-beg `#FFF1CC/#8A5A00`, int `#D8EFDF/#246B42`, adv `#D7E0EC/#14304D`) — do NOT copy OnboardingForm's chip map (banked tier-shift bug lives there only). Battery green: tsc/build ✓, curl D10 proofs (3 sms anchors for joined viewer = n−1, zero digits any encoding for waitlisted — RSC flight payload doubles raw `grep -o "sms:"` counts, use `href="sms:` for DOM truth), redirect traces ✓, 390×844 screenshots passed Ebuka's visual review.

- **D8.2 Phase 2 foundation (committed `83269dd`, pushed)** — per Architect ruling sheet: `globals.css` → D8.2 canonical token sheet (legacy D8 token names kept as aliases); fonts global — `layout.tsx` loads Nunito Sans (`--font-nunito` 400/500/600/700) + Baloo 2 (`--font-baloo2` 600/700), local font defs deleted from `src/app/slot/[id]/page.tsx` (inline `var(--font-*)` refs now cascade from root); HeroText accent `italic` → `font-semibold`; Tier B recolors (ink `#14304D`, CTA `#EE5E00` + `hover:brightness-95`, error `#D64B4B`, decorative `#FF6A00`, success `#4BAE78`, focus sky `#8DBCF1`) across OtpForm, PhoneForm, auth page, create-slot, OnboardingForm, slot detail, Toast. §4 avatar quarantine untouched (HomeClient, mockData, slot owner avatar). §5 guard fired: `lib/slot-preview.ts` NOT edited (imported by quarantined opengraph-image) — its 3 rulings deferred to the avatar/OG dispatch. **Phase 2-T (Architect-authorized follow-up):** Tier A global replace (10 hexes: `#EEF4FA→#E6F0FF`, `#7A9AB8→#5E80A3`, `#DAE7F1→#CFE0F4`, `#E0EEF9→#DCEBFF`, `#0C447C→#15457B`, `#FAF0DC→#FFF1CC`, `#854F0B→#8A5A00`, `#E8F5E9→#D8EFDF`, `#FBEAF0→#D7E0EC`, `#72243E→#14304D`) across src ts/tsx/css excluding the two whole-file quarantines (`src/app/slot/[id]/opengraph-image.tsx`, `lib/slot-preview.ts`) — all 10 verified 0 outside quarantine. Supplementals: HeroText accent final `font-serif font-semibold text-decorative text-4xl`; advanced skill chip `#D7E0EC`/`#14304D` (tier-mapping bug ships as-is per ruling); FILLING header bg `#FF6A001A` (decorative ~10% alpha); Toast + globals comments cleaned. `tsc --noEmit` clean. **§D final residuals:** slot detail venue `#4A7A9E→#14304D` + footer `#A8C4DB→#9DB8D2`; beginner chip greens `#D8EFDF`/`#246B42` (D8.2 int ramp; tier-mapping bug still ships); globals `--color-inset` reversed to `#E6F0FF` (alias→bg, fixes SlotCard inset white-on-white); SlotCard CTA `hover:brightness-95 active:brightness-90`; HeroText doc comment de-DM-Sans'd (lines 3–4 per ruling **+ line 6** extended to clear the dead-font grep — flagged to Architect). Quarantines untouched: opengraph-image, slot-preview (whole file), §4 avatars.

- **Phase 3B (re-verified)** — a11y: skill AND gender chips both now `role="radiogroup"` + `role="radio"` + `aria-checked` (gender was incorrectly `aria-pressed` in first pass). systemPatterns.md: Known Gotchas section added at `memory-bank/systemPatterns.md`. config.toml: `+15555550198` test_otp added. All raw CP output confirmed.

- **Phase 3B** — onboarding form + sequential writes + D2 landings. New files: `src/app/onboarding/OnboardingForm.tsx` (unified client form: first_name, last_name, skill_level, gender chips; `useActionState`; a11y: `aria-pressed`, `role="alert"`, `aria-invalid`, scroll-to-error), `src/app/onboarding/page.tsx` (Server Component: auth-guard, Flow 3 claim write, slot preview fetch), `src/app/onboarding/actions.ts` (submitOnboardingAction: server-side validation → signup_claim → best-effort join_slot → D2 redirects with `?toast=` params), `src/app/join/page.tsx` (Flow 2′ direct join server component). `HomeClient.tsx` updated to read `?toast=` params on mount (joined/waitlisted/welcomed/cancelled/d9) → Zustand showToast + router.replace('/'). Bug fix migration: `20260610120000_fix_signup_claim_jwt_phone.sql` — GoTrue JWT strips leading `+` from phone; claim_token path in signup_claim was comparing raw JWT phone against E.164 DB phone → always mismatch. Fixed by normalising v_jwt_phone before WHERE. All 7 CPs green. config.toml: +15555550033 (WaitlistBob) added to test_otp.

- **Phase 3A** — player auth UI + server-side D2 router (Amendment B). `/auth` route: phone-entry and OTP-verify screens. `sendOtpAction` stateless (Property 4 — no public.users read). `verifyOtpAction`: SSR session via `@supabase/ssr` server client → reads `public.users` (0 or 1 row) → branches server-side: Flow 3 (claim) → `/onboarding?flow=3`, Flow 2 (new+slot) → `/onboarding?flow=2`, Flow 2′ (returning+slot) → `/join`, Flow 1 (new) → `/onboarding?flow=1`, returning → `/`. Pinned banner (`slot_share_preview` anon read, Dallas-local format) on both screens when `?slotId` present. Locked error copy wired. config.toml: `+15555550199` net-new test-OTP (no `public.users` row). All 6 CPs green.
- **Phase 2** — `/slot/[id]` server-rendered detail page (4 states: FORMING/FILLING/FULL/CANCELLED) + `/slot/[id]/opengraph-image` (1200×630, `runtime=nodejs`, Baloo 2 hero + Nunito Sans body). Shared `slot-preview` lib. All times `America/Chicago` (R2). Anon path only — `slot_share_preview` exclusively; zero identity leak. State flip confirmed at 3/6. `generateMetadata`: absolute `og:image`, `og:title` ≤35 chars, `og:description`. All 5 checkpoints green. Copy fix: FORMING bodyCopy + locked subline; FILLING statusCopy="Filling up", count shown separately, locked subline; "Be first in" and "Spots go fast" deleted.
- **M3.5** — audit-trail migration confirming `ends_at` already in `slot_share_preview` from base; P16c updated to assert `ends_at` + `skill_level`. Full 16-proof battery green.
- **Phase 1** — owner-only create-slot (direct RLS INSERT under `slots_insert_owner`; `created_by = current_user_id()`; Dallas-timezone).
- **Phase 0** — local auth spine (`@supabase/ssr`, local test-OTP, bound dev owner).
- **M3.4** — `slots.skill_level` (4-tier CHECK) + `slot_share_preview` projection.
- **D8.2** — new visual identity (brand palette → app tokens); v1 scope unchanged.

## M4 spine (local-first; cloud is the last step)

Phase 0 ✅ · Phase 1 ✅ · Phase 2 ✅ · Phase 3A ✅ · Phase 3B ✅ · Phase 4A ✅ · Phase 4B ✅ · **Phase 4 player surfaces (D13, all-server): Dispatch 1 DB-backed feed ✅ → Dispatch 2 join wiring ✅** · **Phase 5 owner dashboard / cancel: Dispatch 1 `cancel_slot` schema ✅ (D14) → Dispatch 2 dashboard + cancel-sheet UI ✅ (D15, 2026-06-16)** · **M5 player-leave flow + dashboard nav rider ✅ (D16, 2026-06-16, UI-only, branch `feat/m5-player-leave`, awaiting review)** · attendance-review queue DEFERRED · waitlist-leave DEFERRED · Phase 6 Twilio swap + cloud. **Only Phase 6 is Twilio-gated.** (D11 attendance routes built in 4B; the scheduled token-dispatch job + SMS send ride Phase 6 / SMS go-live.)

## Working facts the build needs now

- **Timezone:** owner enters Dallas-local; store via the named-tz cast `'… America/Chicago' → timestamptz` (Postgres applies DST per-date). Civil date = `(starts_at AT TIME ZONE 'America/Chicago')::date`. Never naive-UTC or browser-local — it silently breaks D9 and attendance timing.
- **Slot create:** direct authenticated `.insert()` on `slots`, gated by `slots_insert_owner` (`WITH CHECK is_owner()`). App sets `created_by = current_user_id()` (the `public.users.id`, not the auth uid). No `create_slot` function. No title/description column — skill is structured (`skill_level`).
- **Slot fields:** `sport_id = 'pickleball'` (fixed in v1) · `capacity ∈ {4, 6}` · `gender_category ∈ {open, women, men}` · `skill_level ∈ {beginner, advanced_beginner, intermediate, advanced}`.
- **Venues (3):** `cole-park` (Lakewood) · `churchill-park` (Preston Hollow) · `lake-highlands-north` (Lake Highlands). `venues_select_authenticated` lets authed sessions read the picker.
- **Local auth:** `config.toml` test-OTP — `+15555550101 = "123456"` is the dev owner; a second number exists for a non-owner test. A dummy Twilio provider is enabled so GoTrue loads. Dev owner: `public.users` role `owner`, `auth_user_id` bound at first login.
- **Cloud owner row:** carries Ebuka's REAL number since 2026-06-03 (number lives in the cloud DB only, keep it out of the repo). **BOUND as of 2026-06-30** — `auth_user_id` set (via D2 Flow 3 Path A on first real login under Verify), `claim_token` nulled post-bind. Loud tripwire; never mutate.
- **Cloud schema is current with local — re-verified 2026-07-01 by full parity audit.** A pre-launch security audit caught cloud stuck at the June-13 state (5 of 15 migrations); the 10 pending (D10-A/B, D14, D17, D18, D19, D20) were applied via `supabase db push` and re-audited to full parity: 21 functions, D17 grant matrix exact, chat policies on `is_joined_member`, D19 guard + D10-A redaction confirmed in deployed bodies. Cloud data tables still empty pre-launch. **Lesson: `git push` ships the frontend only — schema ships ONLY via `supabase db push`; run a parity audit before any launch gate.**

## Deployment guardrail

The dummy Twilio creds + test-OTP in `config.toml` are **local-only**. Production SMS is configured in the Supabase Cloud Dashboard. Never push local SMS config to cloud.

## Locked decisions

D1 Zustand · D2 auth (Model C) · D3 schema (+ M3.1–M3.4) · D5 toast states · D7 product mechanics (+ D7.2 form bifurcation, D7.3 optional gender) · D8 design system (+ D8.1 avatar palette, **D8.2 visual identity — supersedes D8 color/type**) · D9 one-game-per-Dallas-day (**D13: client short-circuit dropped, all-server**) · D10 lobby comms (**D10-B lobby wall** — canned + free-text in-app coordination, joined+owner only, host-delete, closes `ends_at`+2h, supersedes the original "no in-app chat in v1"; phones to OWNER only (**D10-A**; peer visibility removed)) · D11 attendance (SMS magic link at `starts_at + 2h`) · D12 `slots.skill_level` patch · **D13 Phase 4 player surfaces all-server** (amends D9, touches D2 Amdt C) · **D14 owner cancel-slot mechanics** (`cancel_slot` RPC + `slot_cancelled` + RLS column inversion) · **D15 owner dashboard + cancel-sheet UI** (`/dashboard`, 2h grace, reused skill ramp, sky selection, Toast-modeled sheet; attendance review deferred) · **D16 player-leave mechanics** (UI-only `LeaveSheet` + `leaveSlotAction`; radiogroup picker w/ separate coded reason + free-text note; time-gated `starts_at > now()` not capacity-gated — amends D7 "no leaving once locked"; owner-only `/dashboard` nav rider; waitlist-leave deferred) · **D17 grant-matrix lockdown** (anon/authenticated EXECUTE buckets; `claim_lookups` → internal/definer-only) · **D18 attendance dispatch DB layer** (`claim_attendance_dispatch` service-role RPC + `attendance_sms_sent_at` dedup, 2h–26h window) · **D19 join_slot past-game guard** (started/ended non-cancelled slots reject; SQLSTATE 55000; "Already started" terminal card) · **D20 public feed** (`get_public_feed` anon read RPC + logged-out `/` front door) · **D21 safety-persistence verified** (lobby-message retention-for-safety already disclosed in privacy policy; gate satisfied).

## Process disciplines

Dispatch-Only (Code acts only on a single paste headed "DISPATCH SPEC") · Raw-Output Verification at every gate · phased checkpoints with human gates · Two-Commit (decision doc separate from implementation) · push is manual (post-validation) · Sketches-Before-Code · load-bearing decisions cross-checked with Gemini.

## Stack

Next.js · Supabase (Postgres + RLS) · `@supabase/ssr` · Zustand · Playwright · Dockerized local Supabase. Repo `github.com/ezikebuka1/Sonye`, local `~/squadup`.
